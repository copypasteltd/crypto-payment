# Lingban Data README

本目录对应拆分分支 `agent-workshop-lingban-data`，保存本地运行时生成的样本数据、bridge 注册信息、对象化结果与 run 工作资料。

This directory maps to the `agent-workshop-lingban-data` branch and stores locally generated runtime samples, bridge registry entries, objectified outputs, and run workspace data.

## 说明 / Notes

- 该目录用于开发、联调、回归验证。
- 其中可能包含脱敏后的运行样本，不应直接视为生产主数据。
- 正式环境应由数据库、对象存储与审计系统作为权威来源。

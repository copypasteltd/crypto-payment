# 灵办词元 Mobile H5 构建验证增量说明（2026-07-09）

## 1. 复查背景

此前 `pnpm -C app/mobile build:h5` 被误判为挂起。  
本次重新复查结论是：构建并未卡死，属于 Windows 本地冷构建耗时较长。

同时补充了一项构建优化：

| 文件 | 调整 |
|---|---|
| `app/mobile/config/index.ts` | 开发态继续使用 `packages/*/src` alias；生产构建默认切回共享包已编译产物，降低跨包源码参与 H5 build 的压力 |

## 2. 复查结果

| 命令 | 结果 |
|---|---|
| `pnpm -C app/mobile build:h5` | 通过 |
| 关键现象 | 先进入 `vite ... transforming...`，随后继续产出完整 `dist/`，最终正常退出 |
| 冷构建耗时 | 首轮观察约 `1m 28s` |
| 优化后再次验证 | 约 `1m 2s` 完成 |

## 3. 产物结果

| 产物 | 结果 |
|---|---|
| `app/mobile/dist/index.html` | 已生成 |
| `app/mobile/dist/css/*` | 已生成 |
| `app/mobile/dist/js/*` | 已生成 |
| `app/mobile/dist/static/images/*` | 已生成 |

## 4. 当前判断

| 维度 | 判断 |
|---|---|
| 构建正确性 | 正常 |
| 构建稳定性 | 当前可稳定完成 |
| 主要问题 | 时延已有下降，但 H5 冷构建仍偏慢，仍需继续优化 |

## 5. 后续优化方向

| 方向 | 说明 |
|---|---|
| 共享包消费策略 | 已切到“开发态源码 alias / 生产态预构建产物”策略，后续继续观察收益并决定是否推广到其他前端端点 |
| 构建缓存 | 评估 Taro/Vite 在 Windows 环境下的缓存复用策略 |
| 依赖裁剪 | 继续压缩前端运行时依赖和入口模块装载体积 |

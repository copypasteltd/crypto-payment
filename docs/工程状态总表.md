# 灵办词元 工程状态总表

## 1. 文档信息

| 字段 | 内容 |
|---|---|
| 文档名称 | 工程状态总表 |
| 统计日期 | 2026-07-12 |
| 适用范围 | 当前 `agent-workshop` 全工作区 |
| 统计口径 | 面向最终完整系统目标，不按 MVP 压缩 |
| 根路径 | `C:\dev\copypaste\agent-workshop` |
| 输出目标 | 统一说明当前系统状态、结构边界、验证基线、阻塞项与下一步工程重点 |

## 2. 当前状态快照

| 维度 | 当前结论 |
|---|---|
| 系统阶段 | 已超过原型阶段，处于高强度工程样机阶段 |
| 代码实现度 | 84% |
| 产品闭环度 | 64% |
| 交付就绪度 | 43% |
| 当前最强模块 | `app/api` 后端正式域 |
| 当前最大缺口 | `session-pack` formal runtime evidence 已落地，剩余缺口转向远端隔离运行链实机证据、双前端更广业务级 E2E、多仓独立交付 |
| 是否可宣称成品交付 | 否 |

## 3. 当前权威证据

| 类别 | 当前事实 |
|---|---|
| 根工作区 Git | 根路径本身不是 Git 仓库 |
| 五个独立仓库 | `app/mobile`、`app/dashboard`、`app/api`、`app/run-worker`、`app/container-bridge` 均有独立 `.git` |
| 当前提交 | `mobile=af0b2ae`、`dashboard=322b4e1`、`api=372bc23`、`run-worker=78b1de7`、`container-bridge=92876c8` |
| 工作树状态 | 五个独立仓库当前均存在未提交变更，尚未形成“可直接推送 GitHub”的收口基线 |
| 共享包 | `api-sdk`、`config`、`contracts`、`credential`、`db`、`domain-models`、`files`、`mcp`、`realtime`、`session-pack`、`shared`、`ui-tokens` 已存在 |
| 缺失共享包 | 无 |
| 根级验证 | 本轮 `pnpm ci:verify` 已通过，且已切换为“单次 build + compiled tests” 口径 |
| 本轮共享包复验 | `packages/db` smoke `25/25`、`packages/files` smoke `3/3`、`packages/shared` smoke `3/3`、`packages/session-pack` smoke `18/18`、`packages/api-sdk` smoke `24/24`、`packages/realtime` build/typecheck 通过 |
| API smoke | 本轮最新全量复验 `57/57` 通过 |
| Run Worker tests | `27/27` |
| Container Bridge tests | `23/23` |
| Frontend Playwright | `23/23`，当前为 smoke 主链 + Creator `session-pack` 业务切片 + 双前端文件预览/下载/附件上传发送主链 + Dashboard 批量运行生命周期主链 |
| 本轮前端复验 | `packages/realtime build`、`app/dashboard build`、`app/mobile build:h5`、根级 Playwright `23/23` 与 `pnpm ci:frontend` 已通过；双前端已补齐“服务启动 -> 首轮信息收集 -> 审批 / 结构化答案复核” smoke 用例，Dashboard 侧已新增 Creator `session-pack` 敏感位补标、脱敏复核决策、脱敏归档导出审计回刷以及 publish / inherit / rollback 业务回刷用例，并补齐 Dashboard / Mobile `plain-text / office-text / image / pdf / download-only` 文件预览下载矩阵、附件上传/发送主链回归，以及 Dashboard 批次草稿创建 -> 校验 / 启动 / 重试 / 取消生命周期回归，并把重复的 run realtime hook 收口到共享 `@lingban/realtime` |
| 前端回退残留 | 运行时 preview/fallback 主链已收口到权威数据或明确空态；Dashboard 当前主要剩余更深消费态治理、更深 lineage 可视化、批量产品化深化与业务级 E2E，Mobile 主要剩余平台特化与附件跨页面恢复 |
| 本地虚拟化约束 | 当前明确禁止调度任何本地 `docker / wsl`，因此真容器链不以本地环境验收 |

## 4. 系统域状态矩阵

| 系统域 | 当前状态 | 结论 |
|---|---|---|
| 产品主模型 | 已形成工坊 / 服务 / 任务 / Creator / 治理的对象边界 | 主模型已稳定，但资产化核心未落地 |
| Mobile H5 | 工坊、任务、文件、我的主路径已存在 | 工坊/任务/文件/我的主链均已切到权威数据或明确空态，任务对话已补本地 outbox、ws ack、失败重试与恢复入口，任务页已显性展示 `informationCollection` 缺口并接入结构化答案复核，已具备启动 -> 首轮信息收集 -> 审批 / 结构化答案复核的 smoke 骨架，剩余缺口转向平台特化与业务级 E2E |
| Dashboard | Shell、工坊、实例、Creator、治理主壳已存在 | 实例、Creator、治理主链均已切到权威数据或明确鉴权空态，服务启动台已具备批次草稿、批次看板、CSV/XLSX 导入解析、工作表切换、显式字段映射、治理输入、预算预估与正式持久化，消费派生识别、基线差异视图、consumer governance 摘要 / 清单、session governance summary（状态 / 风险 / 下线风险 / 基线差异字段 / 派生计数）、signature governance summary（算法 / key / 验签状态 / 兼容性诊断）、policy summary（动作级放行 / 警告 / 阻断、脱敏复核门禁、覆盖完整性门禁、验签门禁）、archive/runtime 证据摘要与实例跳转已落地，实例页已显性展示 `informationCollection` 缺口并接入结构化答案复核，已具备启动 -> 首轮信息收集 -> 审批 / 结构化答案复核的 smoke 骨架，剩余缺口转向更深 lineage 可视化与业务级 E2E |
| API | auth、workspaces、creator、runs、files、credentials、MCP、billing、quota、me、notifications、search 等主域已落地 | 后端是当前最成熟部分 |
| Run Worker | runtime 物料、队列、Docker create/start/stop/rm、恢复与诊断已落地 | 运行框架基本成立，缺真 daemon 证据 |
| Container Bridge | PTY、CLI、recovery、MCP materializer、remote proxy、control HTTP 已落地 | 桥接框架基本成立，缺真 Codex / 真第三方 MCP 证据 |
| 共享层 | 现有共享包已成形，`db`、`files`、`shared`、`session-pack` 与 `realtime` 初始共享层已落地 | `shared` 已落地最小基础层，更深资产化主链仍缺失 |
| 基础设施 | Dockerfile、compose、local stack 脚本已存在 | 真实联调环境仍未建立证据 |
| 测试体系 | 后端 smoke 与 runtime tests 强，前端 E2E 弱 | 关键业务路径仍缺 UI 级全链验证 |

## 5. 仓库状态总表

| 仓库 | 当前定位 | 代码主干完成度 | 交付就绪度 | 当前结论 |
|---|---|---:|---:|---|
| `app/mobile` | 首发轻用户入口 | 91% | 66% | H5 live 主链已覆盖工坊/任务/文件/我的，任务对话已具备本地回显/outbox/ack/失败重试，任务页已显性展示 `informationCollection` 缺口并接入结构化答案复核，工坊启动 -> 首轮信息收集 -> 审批 / 答案复核 smoke E2E 已补齐，仍缺小程序特化与业务级 E2E |
| `app/dashboard` | Creator / Admin / 重度用户控制台 | 95% | 84% | 实例/Creator/治理 live 主链已成型，`Session` 面板已接通 inherit draft / consumer-derived create / publish / unpublish / rollback / archive download，并补齐消费派生识别、lineage 与基线差异视图、consumer governance 摘要 / 清单、session governance summary（状态 / 风险 / 下线风险 / 基线差异字段 / 派生计数）、signature governance summary（算法 / key / 验签状态 / 兼容性诊断）、policy summary（动作级放行 / 警告 / 阻断、脱敏复核门禁、覆盖完整性门禁、验签门禁）、archive/runtime 证据摘要、信息采集 review 摘要、手工敏感位补标与实例跳转；服务启动台已具备批次草稿、批次看板、CSV/XLSX 导入解析、工作表切换、显式字段映射、治理输入、预算预估以及校验/启动/重试/取消基础动作；实例页已显性展示 `informationCollection` 缺口并接入结构化答案复核，服务启动 -> 首轮信息收集 -> 审批 / 答案复核 smoke E2E 已补齐，文件预览 + 下载 ticket + 附件上传/发送主链回归也已补齐；Creator `session-pack` 敏感位补标 / 脱敏复核 / 脱敏归档导出审计 / publish / inherit / rollback 业务级 E2E 已补齐一条主链；仍缺更深 lineage 可视化、正式持久化与更广业务级 E2E |
| `app/api` | 平台 API 中枢 | 99% | 95% | 正式域最完整，缺共享层与部署实机证据 |
| `app/run-worker` | 运行调度与物料层 | 92% | 67% | 调度能力强，缺真容器验证 |
| `app/container-bridge` | 容器内 Codex/CLI/MCP 桥接层 | 91% | 61% | 桥接能力强，缺真 CLI 与真第三方联调 |

更细的仓库完成度请看：

- [仓库完成度与模块状态表.md](C:/dev/copypaste/agent-workshop/docs/仓库完成度与模块状态表.md)

## 6. 当前已完成的关键事项

| 类别 | 已完成内容 |
|---|---|
| 后端正式域 | auth、workspace、creator、runs、files、credentials、MCP、billing、quota、me、notifications、search 已具备正式代码与 smoke |
| 共享数据层起盘 | `@lingban/db` 已具备 manager、migration、transaction helper、auth/creator/runs/event/run-files/uploads/query/bridge/billing/quotas/workshops/notifications/me-recent/me-favorites/batch-runs/search/session-archives/credentials/mcp 契约，以及对应 PostgreSQL 共享实现 |
| 运行框架 | worker 的 runtime 物料、恢复、DLQ、ops HTTP 与 bridge 的 CLI/recovery/proxy/materializer 已建立 |
| 双前端正式工程 | Dashboard 固定 `React + Vite`，Mobile 固定 `Taro`，工程身份已冻结 |
| 主题与设计基础 | `@lingban/ui-tokens` 已接入双端主题 |
| 搜索/最近活动 | 双前端已接入 `/v1/search`、`/v1/search/suggestions`、`/v1/me/recent` |
| 凭证与治理 | Vault、AWS KMS、callback signing、MCP policy、probe、audit 主链已进入代码与 smoke |
| 文件链路 | 上传、ticket、预览、冷归档、安全扫描、下载授权主链已存在 |
| Session archive 存储链 | session archive 字节已切入 `objectStore`，元数据索引已支持 `file/postgres` 双轨，且保留 legacy 本地归档兼容读取；`runtime-evidence.json`、`runtimeEvidenceSummary`、归档级 runtime evidence 元数据索引与 Creator 可视化复核链已落地；当前剩余缺口集中在生产权威源切换、真实对象存储/S3 联调与远端隔离运行时 live consumption 实机证据 |
| CI 验证链收敛 | 根级 `ci:backend / ci:runtime / ci:verify` 已收敛到单次编译后复用 `test:*:compiled`，消除 `build:backend -> test:smoke -> build:backend` 递归构建 |

## 7. 当前未完成的关键事项

| 级别 | 未完成项 | 当前影响 |
|---|---|---|
| P0 | `packages/db` | 已落地 manager / migration / transaction helper、auth/creator/runs/event/run-files/uploads/query/bridge/billing/quotas/workshops/notifications/me-recent/me-favorites/batch-runs/search/session-archives/credentials/mcp 契约、对应 PostgreSQL 共享实现与 seed/reset CLI，但深度 query repository / read-model 收敛 / seed 数据治理仍未完成 |
| P0 | `session-pack` 生产闭环 | `packages/session-pack` 已具备 manifest schema、pack/unpack、bundle/directory 校验、archive read/write round-trip、structured workspace-base archive/restore、slot schema、redaction map schema、文件级 hash 校验，以及 canonical manifest signature 生成/验真；`app/api` 已补齐 `/v1/sessions`、`/v1/sessions/:sessionVersionId`、`POST /v1/sessions/import`、`GET /v1/sessions/:sessionVersionId/archive`、`POST /v1/sessions/:sessionVersionId/inherit`、`POST /v1/sessions/:sessionVersionId/publish`、`POST /v1/sessions/:sessionVersionId/rollback`、`POST /v1/sessions/:sessionVersionId/unpublish`、`POST /v1/sessions/:sessionVersionId/redaction-map` 与 `POST /v1/sessions/:sessionVersionId/redaction-review`，并在 imported/runtime-derived/generated/inherit/internal runtime-fallback 链路接入签名策略；run `informationCollection` 已正式持久化结构化 `slotValues` 与 `answers`，其 review 摘要已纳入 runtime-derived session-pack 导出、`/v1/sessions/:sessionVersionId` 详情与 Dashboard Creator `Session` 面板，并已补齐答案来源追踪、人工修订链与当前生效版本标识，consumer governance 摘要也已纳入 detail 消费链与 Creator 清单展示，session governance summary（状态 / 风险 / 下线风险 / 基线差异字段 / 派生计数）、signature governance summary（算法 / key / 验签状态 / 兼容性诊断）与 policy summary（动作级放行 / 警告 / 阻断、脱敏复核门禁、覆盖完整性门禁、验签门禁）也已纳入正式 detail、SDK、Creator 页面与 smoke/E2E；后端已对 redacted export / publish / inherit / rollback / unpublish 接入统一策略评估，并在命中阻断时返回正式治理错误；redaction coverage、preview 命中摘要、redaction review 记录、archive export audit 与手工敏感位补标也已纳入 session detail 与 Dashboard Creator 可视化复核，Dashboard Creator `Session` 面板现已支持 redaction rule draft / edit / remove / save 写链、敏感位补标写链、正式 redaction review 决策以及导出审计回看，internal run archive 也已改为按 run 精确导出；正式 archive 已优先读取 imported 与 matching run 的 runtime-derived snapshot，无法命中时再退回 generated，持久化字节已切入 `objectStore`，元数据索引也已支持 `file/postgres` 双轨；archive 导出链已支持 `GET /v1/sessions/:sessionVersionId/archive?redact=true`，Dashboard Creator 也已补齐“下载脱敏归档”；本轮已补齐 `runtime-evidence.json`、`runtimeEvidenceSummary`、归档级 runtime evidence 索引落盘、SDK 解析、API smoke 与 Creator runtime evidence 卡片；剩余缺口集中在远端隔离运行时 live consumption 实机证据 |
| P0 | 真 Docker per run 联调 | 运行隔离价值无法证实 |
| P0 | 容器内真实 Codex CLI 验证 | 核心运行链缺实机证据 |
| P0 | 双前端业务级验收 | 运行时 preview/fallback 已完成主链收口，双前端已补齐启动 -> 首轮信息收集 -> 审批 / 结构化答案复核 smoke，Dashboard Creator 也已补齐 publish / inherit / rollback 业务主链，Dashboard / Mobile `plain-text / office-text / image / pdf / download-only` 文件预览下载矩阵、附件上传/发送主链与 Dashboard 批量运行生命周期主链也已纳入回归；剩余重点转为真实 runtime 参与的业务级 E2E |
| P0 | 五仓库脱离根工作区独立构建方案 | 当前远端仓库并非真正独立可交付 |
| P1 | `packages/files` 深化与 `packages/shared` | `files` 已落地 path boundary、logical path、mime/preview 推断与 Office 文本抽取最小共享层；`shared` 已落地 `toErrorMessage / nowIso / buildAtomicTempPath` 并接入 API、worker、bridge；对象存储、download ticket、retention 以及 logger/id/retry/telemetry/feature flag 等更深公共基础设施仍待继续下沉 |
| P1 | 批量启动与批次看板产品化 | 基础批次域、CSV/XLSX 导入解析、工作表切换、显式字段映射、治理输入、预算预估与正式持久化已在，仍缺更深执行治理 |
| P1 | 前端统一组件层与状态层 | 双前端页面仍有大量内嵌实现 |
| P1 | 前端埋点与漏斗分析链 | 产品增长与行为分析能力为空 |
| P1 | 小程序 `weapp/alipay` 特化 | 移动端正式多端输出未完成 |
| P1 | 监控、发布、回滚、上线 Gate | 生产交付能力不足 |

## 8. 当前阻塞项

| 阻塞项 | 当前事实 | 影响范围 |
|---|---|---|
| 本地虚拟化环境禁用 | 真容器链无法以本地环境验证 | runtime、bridge、前后端联调、上线验收 |
| `session-pack` 生产闭环未完成 | Creator 与消费侧已具备敏感位补标、脱敏规则、治理摘要、动作级自动阻断/告警、下线风险识别、签名兼容性诊断、key 窗口 / 分发治理与归档审阅主链，剩余缺口收敛到远端消费证据 | 产品核心能力、Creator、消费继承、运行物化 |
| `packages/db` 仍未完成最终收敛 | 深度 query model、正式 read-model 边界与 seed 数据治理仍缺位 | 架构边界、拆仓交付、长期维护 |
| 根工作区非 Git | 根级文档、共享包、infra 缺统一远端 | 交付与协作边界 |
| 前端剩余缺口已转向体验与验收 | 运行时兜底已收口，剩余为 Mobile 平台特化与附件跨页面恢复、Dashboard 批次显式映射/预算/lineage 与双端业务级 E2E | Dashboard、Mobile 验收 |

## 9. 当前验证基线

| 验证主题 | 当前结果 | 说明 |
|---|---|---|
| 根级回归 | 通过 | `pnpm ci:verify` 本轮通过，包含 backend、runtime、frontend 全链基础门禁，且已验证 compiled-test 口径可替代递归构建链 |
| 构建级健康 | 通过 | 本轮复验了 Dashboard lint/build、Mobile build:h5、`app/api build:local`，以及 `packages/db`、`packages/session-pack`、`packages/api-sdk` 构建链 |
| API smoke | 通过 | 本轮最新全量复验 `57/57` 通过 |
| Worker / Bridge tests | 通过 | `27/27`、`23/23` |
| Frontend E2E smoke | 通过 | `23/23`，当前覆盖 Dashboard 基础路由、深批次路由、实例 `plain-text / office-text / image / pdf / download-only` 文件预览/下载矩阵、实例附件上传/发送、批次预算估算、批次草稿创建 -> 校验 / 启动 / 重试 / 取消生命周期、服务启动 -> 首轮信息收集、审批直连、结构化答案复核、Creator `session-pack` 敏感位补标、脱敏复核决策、脱敏归档导出审计回刷与 publish / inherit / rollback 业务回刷，以及 Mobile 任务/文件导航、`plain-text / office-text / image / pdf / download-only` 文件预览/下载矩阵、任务附件上传/发送、发送失败重试链、服务启动 -> 首轮信息收集、审批直连与结构化答案复核 |
| 真 Docker 联调 | 未验证 | 当前禁止使用本地 `docker / wsl`，且没有替代远端容器验收环境 |
| 真全链联调 | 未验证 | 缺 API -> worker -> container runtime -> bridge -> Codex -> files -> 双前端实机证据 |

## 10. 当前权威文档索引

| 场景 | 当前优先阅读文档 |
|---|---|
| 最终交付差距与开发项 | [最终交付差距与开发项-2026-07-12.md](C:/dev/copypaste/agent-workshop/docs/最终交付差距与开发项-2026-07-12.md) |
| 系统差距审计 | [系统实现差距审计-2026-07-12.md](C:/dev/copypaste/agent-workshop/docs/系统实现差距审计-2026-07-12.md) |
| 仓库完成度 | [仓库完成度与模块状态表.md](C:/dev/copypaste/agent-workshop/docs/仓库完成度与模块状态表.md) |
| 联调验收 | [联调与验收清单.md](C:/dev/copypaste/agent-workshop/docs/联调与验收清单.md) |
| Gate 判断 | [里程碑Gate打分表.md](C:/dev/copypaste/agent-workshop/docs/里程碑Gate打分表.md) |
| 后端总体设计 | [后端设计文档.md](C:/dev/copypaste/agent-workshop/docs/后端设计文档.md) |
| 前端总体需求 | [前端需求.md](C:/dev/copypaste/agent-workshop/docs/前端需求.md) |

## 11. 当前工程结论

当前代码库已经具备较完整的平台雏形，最难的主框架工作基本完成。  
当前不能宣称“最终成品已交付”，原因集中在四个层面：

1. 会话资产化核心缺失  
2. 真容器运行证据缺失  
3. 双前端业务级 E2E 与平台特化未完成  
4. 生产级交付链未闭环  

后续推进顺序应固定为：

1. `packages/db`
2. `packages/session-pack`
3. 远端隔离运行链 + 真 Codex CLI 联调
4. 补齐双前端业务级 E2E 与平台特化
5. 建立部署、回滚、监控与上线 Gate

# 灵办词元 联调与验收清单

## 1. 文档信息

| 字段 | 内容 |
|---|---|
| 文档名称 | 联调与验收清单 |
| 文档类型 | Integration & Acceptance Checklist |
| 适用范围 | `agent-workshop` 全工作区 |
| 基准日期 | 2026-07-12 |
| 关联文档 | `docs/工程状态总表.md`、`docs/开发任务拆解总表.md` |
| 使用方式 | 用于本地联调、阶段验收、上线前检查 |

## 2. 联调阶段定义表

| 阶段 | 目标 | 适用仓库 |
|---|---|---|
| L1 | 单仓构建与基础运行通过 | 全仓 |
| L2 | 后端主链联通 | `app/api`、`app/run-worker`、`app/container-bridge` |
| L3 | Dashboard 联调通过 | `app/dashboard` + 后端 |
| L4 | Mobile H5 联调通过 | `app/mobile` + 后端 |
| L5 | Runtime/文件/审批/MCP 联调通过 | 全链路 |
| L6 | 企业治理与上线验收 | 全链路 |

## 3. 本地联调环境准备表

| 项目 | 要求 | 当前状态记录 | 验收方式 |
|---|---|---|---|
| Node.js | `22` | 已验证：`v22.14.0` | `node -v` |
| pnpm | `10.0.0` | 已验证：本机 `11.7.0`，与根工作区 `packageManager` 不一致 | `pnpm -v` |
| Docker Engine | 可用 | 客户端存在：`20.10.16`；Docker daemon 未运行 | `docker version` |
| PostgreSQL | 可用 | 未验证，本地未见运行证据 | 本地连接成功 |
| Redis | 可用 | 未验证，本地未见运行证据 | ping 成功 |
| Object Storage | MinIO/S3 兼容 | 未验证，本地未见运行证据 | bucket 可读写 |
| Codex CLI | 容器内可执行 | runner Dockerfile 已固定安装 `@openai/codex@0.144.1`，entrypoint 已补 `codex` 缺失前置校验；尚无容器内运行证据 | `codex --help` 或启动成功 |
| Playwright Chromium | runner image 内可用 | Dockerfile 已声明安装 `@playwright/test` 与 Chromium，未做镜像内实测 | browser install 成功 |

## 4. 服务启动顺序检查表

| 顺位 | 服务/组件 | 启动条件 | 验证点 | 当前状态 |
|---|---|---|---|---|
| 1 | PostgreSQL | 环境变量已配置 | 可连接 | 未启动验证 |
| 2 | Redis | 环境变量已配置 | 可连接 | 未启动验证 |
| 3 | Object Storage | bucket 已初始化 | 可上传对象 | 未启动验证 |
| 4 | `app/api` | DB/Redis 基础可用 | `/health` 返回成功 | 编译已验证，服务未实启 |
| 5 | `app/run-worker` | API/Redis 可用 | worker 可消费队列 | 仅库层与 runtime 物料已验证，常驻消费未建立 |
| 6 | Runner Image | 镜像构建完成 | 能启动 bridge CLI | Dockerfile 半成品已存在，未构建联调 |
| 7 | `app/dashboard` | API baseUrl 已配置 | 页面可打开并请求 API | 构建已验证，页面联调未启动 |
| 8 | `app/mobile` H5 | API baseUrl 已配置 | 页面可打开并请求 API | H5 构建已验证，页面联调未启动 |

## 5. 基础健康检查表

| 检查项 | 目标 | 期望结果 | 当前状态 |
|---|---|---|---|
| API `/health` | 基础服务存活 | `status=ok` | 未开始 |
| Runs List | 基础读接口可用 | `GET /v1/runs` 返回 200 | 未开始 |
| Create Run | 基础写接口可用 | `POST /v1/runs` 返回 run snapshot | 未开始 |
| WebSocket | 实时通道可用 | 可收到 `runs.snapshot` | 未开始 |
| SSE | 回退通道可用 | 可收到 `runs.snapshot` | 未开始 |
| File Tree | 文件树可读取 | `/files/tree` 返回数组 | 未开始 |
| File Read | 文本文件可读取 | `/files/read` 返回内容 | 未开始 |
| File Download | 下载链路可用 | `/files/download` 可拿到文件流 | 未开始 |

## 6. Dashboard 联调检查表

| 页面/能力 | 检查项 | 期望结果 | 当前状态 |
|---|---|---|---|
| 工坊列表 | 页面加载 | 列表来自真实 API | 未开始 |
| 工坊详情 | 服务信息展示 | 详情来自真实 API | 未开始 |
| 实例列表 | 多实例展示 | 列表来自真实 run 数据 | 未开始 |
| 实例详情 | 快照加载 | 进入详情页即拿到 snapshot | 未开始 |
| 实例对话 | 发送消息 | 用户消息成功进入 run | 未开始 |
| 实例实时流 | 实时更新 | agent/system 事件实时刷新 | 未开始 |
| 实例文件页 | 文件树展示 | 当前 target path 文件可见 | 未开始 |
| 文件下载 | 下载结果物 | 点击后可下载文件 | 未开始 |
| Creator 包页 | 真数据接入 | 页面来自真实 API | 未开始 |
| Creator 治理页 | 成员/凭证/策略/审计页 | 各页接口正常 | 未开始 |

## 7. Mobile H5 联调检查表

| 页面/能力 | 检查项 | 期望结果 | 当前状态 |
|---|---|---|---|
| 工坊首页 | 页面加载 | 来自真实工坊数据 | 未开始 |
| 服务详情页 | 展示服务信息 | 数据来自真实接口 | 未开始 |
| 任务列表 | 多任务列表 | 来自真实 run 列表 | 未开始 |
| 任务对话页 | 进入详情 | 立即拿到 snapshot | 未开始 |
| 任务摘要折叠 | 交互正确 | 展开/收起行为正常 | 未开始 |
| 实时消息 | WebSocket/SSE 更新 | 新消息实时出现 | 未开始 |
| 审批动作 | 审批提交 | 状态变化并反馈到对话 | 未开始 |
| 文件页 | 路径切换 | 不同 path 可浏览 | 未开始 |
| 文件下载 | 下载结果物 | H5 下可直接下载 | 未开始 |
| 我的/空间切换 | 切换 workspace | 切换后数据域变化正确 | 未开始 |

## 8. Run 主链联调检查表

| 步骤 | 检查项 | 强证据 | 当前状态 |
|---|---|---|---|
| 1 | 创建 run | API 返回 `runId` 与初始 prompt | 未开始 |
| 2 | 生成 runtime 物料 | `runtime/` 下 7 类文件齐全 | 未开始 |
| 3 | worker 接单 | worker 日志出现 start-run | 未开始 |
| 4 | 真容器启动 | `docker ps` 可见容器 | 未开始 |
| 5 | bridge 注册 | API internal register 成功 | 未开始 |
| 6 | Codex 会话启动 | 收到 `STARTING` -> `RUNNING` | 未开始 |
| 7 | 用户消息进入 PTY | 对话中看到 echo/result | 未开始 |
| 8 | 文件变更回流 | files synced/file changed 事件出现 | 未开始 |
| 9 | artifact 回流 | artifact ready 事件出现 | 未开始 |
| 10 | run 结束清理 | `SUCCEEDED/FAILED/CANCELLED` 且容器停止 | 未开始 |

## 9. Realtime 联调检查表

| 维度 | 检查项 | 期望结果 | 当前状态 |
|---|---|---|---|
| WebSocket 首帧 | 连接后立即收到 snapshot | 成功 | 未开始 |
| WebSocket 消息发送 | `runs.sendMessage` 后收到 ack | 成功 | 未开始 |
| WebSocket 审批 | `runs.approve` 后状态推进 | 成功 | 未开始 |
| WebSocket 取消 | `runs.cancel` 后状态变更 | 成功 | 未开始 |
| SSE 首帧 | 打开 stream 后收到 snapshot | 成功 | 未开始 |
| SSE backlog | 历史事件可回放 | 成功 | 未开始 |
| 断线恢复 | 重连后 snapshot 正确 | 成功 | 未开始 |
| 双端一致性 | Dashboard/H5 同看同一 run | 快照与事件一致 | 未开始 |

## 10. 文件与产物联调检查表

| 维度 | 检查项 | 期望结果 | 当前状态 |
|---|---|---|---|
| 文件树 | `files/tree` | 能返回 target path 范围内目录树 | 未开始 |
| 文件 stat | `files/stat` | 文件元信息正确 | 未开始 |
| 文本读取 | `files/read` | 文本内容可预览，超长标记 truncation | 未开始 |
| 边界校验 | 越界 path 访问 | 返回 `FILE_PATH_INVALID` | 未开始 |
| 目录读取 | 目录 read/download | 被拒绝 | 未开始 |
| 下载 | 单文件下载 | 浏览器可拿到正确文件 | 未开始 |
| artifact 上传 | outputs 上传对象存储 | 有 object key / downloadUrl | 未开始 |
| artifact 回写 | 前端可见 artifact | 结果页/文件页可见 | 未开始 |

## 11. 审批链路联调检查表

| 步骤 | 检查项 | 期望结果 | 当前状态 |
|---|---|---|---|
| 1 | run 进入 `WAITING_APPROVAL` | 状态推送到前端 | 未开始 |
| 2 | 前端显示审批模块 | 用户可见审批请求 | 未开始 |
| 3 | 用户同意 | API 写入审批结果 | 未开始 |
| 4 | bridge 收到 approve 命令 | PTY 收到审批文本 | 未开始 |
| 5 | run 继续执行 | 状态回到 `RUNNING` | 未开始 |
| 6 | 用户拒绝 | run 进入 `CANCELLED` 或终止态 | 未开始 |

## 12. Credential 与 MCP 联调检查表

| 维度 | 检查项 | 期望结果 | 当前状态 |
|---|---|---|---|
| Credential Env 注入 | env 模式 secret | 容器内环境变量存在，前端不暴露 | 未开始 |
| Credential File 注入 | file 模式 secret | 容器内只读文件存在 | 未开始 |
| MCP First-party | 内置 MCP 可启动 | 可正常响应 | 未开始 |
| MCP Workspace-managed | 空间管理 MCP 可启动 | 绑定与策略正确 | 未开始 |
| MCP Third-party | 第三方 MCP 接入 | 有策略校验、网络限制、审计 | 未开始 |
| 审批要求 | `approvalRequired=true` 的 MCP | 触发用户审批 | 未开始 |
| 脱敏返回 | 凭证读取接口 | 不返回明文 | 未开始 |

## 13. 容器隔离验收表

| 验收项 | 强证据 | 当前状态 |
|---|---|---|
| 每个 run 独立容器 | 不同 run 有不同 container name / id | 未开始 |
| 独立 target path | 不同 run 的挂载路径不同 | 未开始 |
| 独立 HOME / CODEX_HOME | 容器内环境变量与 mount 独立 | 未开始 |
| 独立 secrets | 不同 run secret mount 不互通 | 未开始 |
| 独立 browser profile | 不同 run 浏览器 profile 隔离 | 未开始 |
| 容器停止后清理 | `docker ps` 不再残留运行容器 | 未开始 |

## 14. 数据一致性验收表

| 维度 | 检查项 | 期望结果 | 当前状态 |
|---|---|---|---|
| API 快照一致性 | 同一 run 多次读取 | snapshot 一致 | 未开始 |
| Event -> Snapshot 投影 | 收到 event 后 snapshot 更新正确 | 一致 | 未开始 |
| Dashboard/H5 一致性 | 两端看同一 run | 状态、消息、文件一致 | 未开始 |
| 文件索引一致性 | file tree 与 artifact/下载一致 | 一致 | 未开始 |
| 持久化恢复一致性 | 服务重启后 run 状态正确恢复 | 一致 | 未开始 |

## 15. 故障演练清单表

| 场景 | 操作 | 期望结果 | 当前状态 |
|---|---|---|---|
| API 重启 | 运行中途重启 API | 已持久化 run 可恢复读取 | 未开始 |
| Worker 崩溃 | 运行中 kill worker | run 进入可观测异常状态 | 未开始 |
| bridge 异常退出 | kill bridge 进程 | run 失败并记录原因 | 未开始 |
| Codex 非零退出 | 模拟命令失败 | run `FAILED` 且有 reason | 未开始 |
| 对象存储失败 | 上传失败 | run 保持失败可解释状态 | 未开始 |
| 第三方 MCP 不可达 | 模拟远端不可用 | 有错误事件与审计 | 未开始 |
| 前端 WS 断开 | 关闭 WebSocket | 自动回退或可恢复 | 未开始 |

## 16. 安全与治理验收表

| 维度 | 检查项 | 期望结果 | 当前状态 |
|---|---|---|---|
| 鉴权 | 未登录访问受保护接口 | 被拒绝 | 未开始 |
| 空间边界 | A 空间不可读 B 空间 run | 被拒绝 | 未开始 |
| 文件边界 | 越界读取 target path | 被拒绝 | 未开始 |
| 凭证脱敏 | 凭证接口无明文泄漏 | 成功 | 未开始 |
| MCP 策略 | 高风险 MCP 无授权不可运行 | 成功 | 未开始 |
| 审计 | 关键动作均有日志 | 成功 | 未开始 |

## 17. 发布前 Gate 表

| Gate | 内容 | 达成条件 | 当前状态 |
|---|---|---|---|
| Gate 1 | 构建 | 所有正式仓库 build/typecheck 通过 | 部分实现：当前已验证共享包、后端主链、Dashboard、H5 构建通过 |
| Gate 2 | 主链 | 创建 run -> 对话 -> 文件 -> 结束主链通过 | 未开始 |
| Gate 3 | 隔离 | Docker per run 隔离证据齐全 | 未开始 |
| Gate 4 | 安全 | auth、workspace、credential、MCP 治理通过 | 未开始 |
| Gate 5 | 审计 | 核心动作有审计 | 未开始 |
| Gate 6 | 观测 | 日志、错误、关键指标可追踪 | 未开始 |
| Gate 7 | 回滚 | 前后端与 DB 回滚方案已验证 | 未开始 |

## 18. 联调证据归档表

| 证据类型 | 建议归档内容 |
|---|---|
| 构建证据 | build/typecheck 日志 |
| API 证据 | curl/httpie 响应样例、OpenAPI 截图 |
| 实时证据 | WebSocket/SSE 消息日志 |
| 运行时证据 | runtime 目录文件、container inspect 输出 |
| 文件证据 | 下载文件、预览截图、artifact URL |
| 前端证据 | Dashboard/H5 联调截图与录屏 |
| 安全证据 | 权限拒绝、脱敏响应、审计日志 |
| 发布证据 | CI 日志、镜像 tag、部署记录 |

## 19. 当前验收结论快照表

| 项目 | 现状结论 |
|---|---|
| Dashboard 构建 | 已通过 |
| Mobile H5 构建 | 已通过 |
| 共享包构建 | 已通过 |
| 后端主链编译 | 已通过 |
| 实际测试用例 | 已建立；后端 smoke、runtime tests、前端 Playwright smoke 已存在，并要求先执行 `node tests/e2e/build-frontends.mjs` 生成 E2E 专用静态产物 |
| Session Pack Archive 主链 | imported、runtime-derived、runtime-fallback、inherit、publish、rollback、unpublish、lineage 均已有 smoke 证据 |
| Docker daemon | 客户端存在，daemon 未运行 |
| Docker 真执行验收 | 未开始，当前受 daemon/真实执行链阻塞 |
| Auth/Workspace 验收 | 已有 API smoke 与前端接线，缺真实多用户端到端联调证据 |
| MCP/Credential 治理验收 | 已有治理代码与 smoke，缺 live external MCP + Docker 运行证据 |
| 前后端真实联调 | 已有前端 smoke，已覆盖工坊启动 -> 首轮信息收集与审批直连；仍缺真实后端/runtime/files/Creator 参与的全链证据 |

## 20. 当前已验证基线表

| 维度 | 当前已验证事实 | 证据 |
|---|---|---|
| 本机 Node | `v22.14.0` | `node -v` |
| 本机 pnpm | `11.7.0` | `pnpm -v` |
| 共享包构建 | `pnpm build:shared` 通过 | 根工作区构建日志 |
| 后端主链构建 | `pnpm build:backend` 通过 | 根工作区构建日志 |
| 根级验证 | `pnpm ci:verify` 通过，且已切到 compiled-test 口径 | 根工作区验证日志 |
| Dashboard 构建 | `pnpm -C app/dashboard build` 通过 | Dashboard 构建日志 |
| Mobile H5 构建 | `pnpm -C app/mobile build:h5` 通过 | H5 构建日志 |
| API smoke | 最近完整绿灯基线已刷新为 `57/57`，当前权威口径以 `57/57` 为准 | `app/api test:smoke` |
| Runtime tests | `app/run-worker` `27/27`，`app/container-bridge` `23/23` | runtime 测试日志 |
| Frontend E2E | `23/23` 通过；当前覆盖 Dashboard 基础路由、Dashboard 深批次路由、实例 `plain-text / office-text / image / pdf / download-only` 文件预览/下载矩阵、实例附件上传/发送、批次预算估算、批次草稿创建 -> 校验 / 启动 / 重试 / 取消生命周期、服务启动 -> 首轮信息收集、审批直连、结构化答案复核、Dashboard Creator `session-pack` 敏感位补标、脱敏复核决策与脱敏归档导出审计，以及 Mobile 任务/文件导航、`plain-text / office-text / image / pdf / download-only` 文件预览/下载矩阵、任务附件上传/发送、发送失败重试链、我的页空间摘要、服务启动 -> 首轮信息收集、审批直连与结构化答案复核，仍属于 smoke 主链 + Creator 业务切片级 | `tests/e2e/specs/*.mjs` |
| Session Pack Archive | `tests/session-pack-archive.smoke.test.mjs` 通过，已覆盖 imported、runtime-derived、runtime-fallback、inherit、publish、rollback、unpublish、lineage | session-pack archive smoke 日志 |
| API 主链代码 | `runs / realtime / files / internal bridge / auth / workspaces / creator / credentials / mcps` 已存在 | `app/api` 代码与 smoke tests |
| Runtime 物料链 | `runtime-config.json`、`bridge-context.*.json`、`mcp-config.json`、`secret-manifest.json`、`container-launch-plan.json` 已可生成 | `app/run-worker` 代码 |
| Bridge CLI | CLI、PTY、health/control、artifact 事件已存在 | `app/container-bridge` 代码 |

## 21. 当前明确阻塞项表

| 阻塞项 | 当前事实 | 影响阶段 |
|---|---|---|
| Docker daemon 未运行 | `docker version` 只能拿到 client，server 不可连接 | L2 / L5 / L6 |
| 共享 `packages/db` 主链未收敛 | `packages/db` 已建立最小共享骨架，但正式 schema / repository / transaction 尚未收敛完成 | L2 / L3 / L4 / L6 |
| Redis 未验证 | worker 常驻消费尚未建立 | L2 / L5 |
| 对象存储未验证 | artifact 上传链未闭环 | L5 / L6 |
| 真多用户联调证据缺失 | auth/workspace 正式域已存在，但尚未完成多用户、多空间、跨端实机联调 | L3 / L4 / L6 |
| Session Pack 生产闭环未完成 | `packages/session-pack` 已接入发布、导入导出、Worker 运行消费主链，且已补齐手工敏感位补标、治理摘要、动作级自动阻断/告警、下线风险识别、基线差异审计、签名兼容性诊断、多 key 验签窗口、签名 key 分发治理，以及 `runtime-evidence.json` / `runtimeEvidenceSummary` / Creator runtime evidence 可视化；剩余缺口集中在远端隔离运行时 live consumption 实机证据 | L3 / L5 / L6 |
| Creator 全链证据缺失 | Creator 正式 API 与 smoke 已存在，但缺 pack/live runtime/页面全链联调 | L3 / L6 |

## 22. 联调执行顺序表

| 顺位 | 联调主题 | 进入条件 | 完成判定 |
|---|---|---|---|
| 1 | 构建级健康校验 | 当前工作区代码稳定 | 根构建、Dashboard 构建、H5 构建通过 |
| 2 | API 单机主链联调 | API 可启动 | `/health`、`/v1/runs`、`/ws/runs` 可用 |
| 3 | runtime 物料联调 | API 可创建 run | `runtime/` 目录与 7 类文件齐全 |
| 4 | Docker 真执行联调 | daemon、worker、runner image 可用 | 单个 run 真容器可启动和结束 |
| 5 | Dashboard 真接口联调 | API 主链稳定 | 工坊/实例/文件/对话联调通过 |
| 6 | H5 真接口联调 | API 主链稳定 | 工坊/任务/文件/对话联调通过 |
| 7 | Credential / MCP 联调 | broker 与 registry 存在 | 注入、审批、策略、审计链成立 |
| 8 | 企业治理与上线 Gate | auth/audit/CI-CD 存在 | 发布前 Gate 全通过 |

## 23. 联调证据命令矩阵表

| 主题 | 最低命令或动作 | 目标证据 |
|---|---|---|
| API 存活 | `curl http://127.0.0.1:3100/health` | `status=ok` |
| Run 创建 | `POST /v1/runs` | `runId`、初始 snapshot |
| WebSocket | 建立 `ws://127.0.0.1:3100/ws/runs/:runId` | `runs.snapshot` 首帧 |
| SSE | 打开 `/v1/runs/:runId/stream` | `runs.snapshot` 首帧 |
| Runtime 物料 | 检查 `runtime/` 目录 | 7 类文件齐全 |
| Docker 真执行 | `docker ps` / `docker inspect <id>` | 运行中容器与挂载信息 |
| 文件树 | `GET /v1/runs/:runId/files/tree` | 目录树数组 |
| 文件下载 | 浏览器或 curl 下载 | 文件流与内容校验 |
| Dashboard 联调 | 打开实例页并发送消息 | 页面截图 + 请求/事件日志 |
| H5 联调 | 打开任务页并发送消息 | 页面截图 + 请求/事件日志 |

## 24. 当前联调准备度打分表

| 维度 | 当前评分 | 满分 | 说明 |
|---|---:|---:|---|
| 构建准备度 | 5 | 5 | 双前端、共享包、后端主链均已构建通过 |
| 本地环境准备度 | 2 | 5 | Node/pnpm 已验证，本地 `docker / wsl` 验收不执行，远端隔离运行环境、DB、Redis、Object Storage 证据未就位 |
| API 主链准备度 | 4 | 5 | 代码与 smoke 已较完整，仍缺进程级全链联调 |
| Runtime 准备度 | 3 | 5 | 物料链和 bridge CLI 已存在，远端隔离运行链未验证 |
| 前端联调准备度 | 4 | 5 | 页面壳、API 接线与路由 smoke 已存在，且已覆盖启动 -> 首轮信息收集 -> 审批直连 smoke；缺真实 runtime 参与的业务级 E2E 证据 |
| 治理准备度 | 3 | 5 | auth / credential / MCP / quota / billing 正式域已存在，缺 live external MCP 与上线级证据 |
| 合计 | 21 | 30 | 当前适合进入“远端隔离运行链 + 双前端业务级 E2E + Creator 全链联调”，不适合宣称最终成品验收 |

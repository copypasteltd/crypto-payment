# 灵办词元 环境与部署清单总表

## 1. 文档信息

| 字段 | 内容 |
|---|---|
| 文档名称 | 环境与部署清单总表 |
| 文档类型 | Environment & Deployment Matrix |
| 适用范围 | `agent-workshop` 全工作区 |
| 基准日期 | 2026-07-08 |
| 关联文档 | `docs/实施排期总表.md`、`docs/联调与验收清单.md`、`docs/里程碑Gate打分表.md` |
| 使用目的 | 统一环境分层、部署组件、配置矩阵、发布顺序、回滚与备份要求 |

## 2. 环境分层总表

| 环境 | 目的 | 主要使用者 | 数据要求 | 稳定性要求 |
|---|---|---|---|---|
| Local Dev | 本地开发与单人验证 | 开发者 | 可重置、可污染 | 低 |
| Shared Dev | 多人共享联调 | 开发者、QA | 可重复初始化 | 中 |
| Staging | 里程碑验收与预发布验证 | QA、Product、架构、运维 | 接近生产 | 高 |
| Production | 正式对外服务 | 全体终端用户 | 真正权威数据 | 最高 |

## 3. 当前环境成熟度表

| 环境 | 当前状态 | 当前证据 | 主要缺口 |
|---|---|---|---|
| Local Dev | 部分可用 | `node -v`=`v22.14.0`、`pnpm -v`=`11.7.0`，五仓与共享包 build 已通过；`infra/docker/docker-compose.local.yml`、`compose.env`、`worker.host.env` 与 API Dockerfile 已落地，`docker compose config` 已通过 | 真正的 `compose up`、API 镜像构建与 run 容器联调尚未完成实机验收 |
| Shared Dev | 未建立 | 无 | 缺环境脚本、缺共享服务规范 |
| Staging | 未建立 | 无 | 缺部署链、镜像链、回滚链 |
| Production | 未建立 | 无 | 缺 auth、audit、CI/CD、发布治理 |

## 4. 部署组件总表

| 组件 | 类型 | 部署形态 | 当前状态 | 目标状态 |
|---|---|---|---|---|
| Dashboard | 静态前端 | 静态资源托管 | 可构建 | 可版本发布 |
| Mobile H5 | 静态前端 | 静态资源托管 | 可构建 | 可版本发布 |
| API | Node 服务 | 常驻进程 / 容器 | 可构建 | 可部署、可横向扩展 |
| Run Worker | Node 服务 | 常驻进程 / 容器 | 半成品 | 独立 worker 部署 |
| Container Bridge | 容器内 runtime | runner image 内进程 | 半成品 | 每 run 独立运行 |
| PostgreSQL | 数据库 | 托管或自管服务 | 本地 compose 已定义 | 正式权威数据层 |
| Redis | 队列/缓存 | 托管或自管服务 | 本地 compose 已定义 | BullMQ 队列底座 |
| Object Storage | 文件对象存储 | S3/MinIO | 本地 compose 已定义 | 产物与文件正式存储 |
| Runner Image Registry | 镜像仓库 | OCI registry | 未建立 | runner image 可版本分发 |

## 5. 服务到环境部署矩阵表

| 组件 | Local Dev | Shared Dev | Staging | Production |
|---|---|---|---|---|
| Dashboard | 是 | 是 | 是 | 是 |
| Mobile H5 | 是 | 是 | 是 | 是 |
| API | 是 | 是 | 是 | 是 |
| Run Worker | 目标需要 | 是 | 是 | 是 |
| PostgreSQL | 目标需要 | 是 | 是 | 是 |
| Redis | 目标需要 | 是 | 是 | 是 |
| Object Storage | 目标需要 | 是 | 是 | 是 |
| Runner Image Registry | 否 | 可选 | 是 | 是 |

## 6. 端口与网络矩阵表

| 组件 | 默认端口/入口 | 当前来源 | 说明 |
|---|---|---|---|
| API HTTP | `3100` | `API_PORT` 默认值 | 前后端默认连接入口 |
| API Health | `/health` | `app/api` | 基础健康检查 |
| Realtime WS | `/ws/runs/:runId` | `app/api` | run 实时通道 |
| Realtime SSE | `/v1/runs/:runId/stream` | `app/api` | WS 回退通道 |
| Bridge Control | `3800` | `LINGBAN_BRIDGE_PORT` 默认值 | 容器内控制/健康用途 |
| Bridge Health | `/health` | `control-http.ts` | bridge 自检入口 |
| Runner Network | `lingban-egress-default` | `LINGBAN_RUNNER_NETWORK` 默认值 | run 容器默认网络 |

## 7. 域名与入口规划表

| 入口 | Local Dev 建议 | Staging 建议 | Production 建议 |
|---|---|---|---|
| Dashboard | `http://127.0.0.1:<vite-port>` | `dashboard-staging.<domain>` | `dashboard.<domain>` |
| Mobile H5 | `http://127.0.0.1:<taro-port>` | `m-staging.<domain>` | `m.<domain>` |
| API | `http://127.0.0.1:3100` | `api-staging.<domain>` | `api.<domain>` |
| Object Storage Console | 本地端口映射 | 内网或 VPN | 内网/受限访问 |
| Admin/Observability | 本地无要求 | `ops-staging.<domain>` | `ops.<domain>` |

## 8. 环境变量分层表

| 变量类别 | Local Dev | Shared Dev | Staging | Production |
|---|---|---|---|---|
| API Host/Port | 本地默认可用 | 环境内指定 | 固定域名/端口 | 固定域名/端口 |
| DB 连接串 | 本地容器或本地实例 | 共享实例 | staging 实例 | production 实例 |
| Redis 连接串 | 本地容器或本地实例 | 共享实例 | staging 实例 | production 实例 |
| Object Storage | 本地 MinIO | 共享 MinIO/S3 | staging bucket | production bucket |
| Runner Image | 本地 tag | dev tag | staging tag | release tag |
| Credential Broker Key | 本地伪值/测试值 | dev secret | staging secret | production secret |
| Auth Secret | 本地伪值 | dev secret | staging secret | production secret |

## 9. Secret 管理矩阵表

| Secret 类型 | Local Dev | Shared Dev | Staging | Production | 当前状态 |
|---|---|---|---|---|---|
| DB 密码 | `.env.local` 或本地容器 | secret store | secret store | secret store | 未正式治理 |
| Redis 密码 | `.env.local` | secret store | secret store | secret store | 未正式治理 |
| Object Storage Key | `.env.local` | secret store | secret store | secret store | 未正式治理 |
| Credential Broker Master Key | 本地测试值 | secret store | secret store | secret store | 未实现 |
| JWT/Auth Secret | 本地测试值 | secret store | secret store | secret store | 未实现 |
| Internal Bridge Auth Token | 本地测试值 | secret store | secret store | secret store | 部分预留 |
| 第三方 MCP 凭证 | 本地测试 credential | secret store | secret store | secret store | 未实现 |

## 10. 数据目录与存储矩阵表

| 数据类别 | 当前路径/介质 | 目标介质 | 生命周期要求 |
|---|---|---|---|
| API 元数据 | `.lingban-data/api` | PostgreSQL | 长期权威存储 |
| Worker runRoot | `.lingban-data/worker/runs/<runId>` | 本地磁盘/卷 | 短中期执行态 |
| Runtime 文件 | `<runRoot>/runtime` | 本地磁盘/卷 | 可重建、需留调试窗口 |
| Inputs | `<runRoot>/inputs` | 本地磁盘 + 对象存储元数据 | 输入留存策略受治理控制 |
| Outputs | `<runRoot>/outputs` | 对象存储 | 结果物需可下载与归档 |
| Logs | `<runRoot>/logs` | 本地磁盘 + 日志系统 | 需保留与检索 |

## 11. 部署顺序表

| 顺位 | 组件 | 进入条件 | 完成判定 |
|---|---|---|---|
| 1 | PostgreSQL | 基础设施可用 | 可连接、可执行 migration |
| 2 | Redis | 基础设施可用 | 可连接、可 ping |
| 3 | Object Storage | 基础设施可用 | bucket 可读写 |
| 4 | API | DB/Redis 基础可用 | `/health` 正常 |
| 5 | Run Worker | API/Redis 可用 | worker 能消费任务 |
| 6 | Runner Image | registry/构建链可用 | 镜像可 pull、可启动 |
| 7 | Dashboard / H5 | API 已稳定 | 页面可连真实 API |

## 12. 本地开发启动矩阵表

| 场景 | 推荐启动顺序 | 备注 |
|---|---|---|
| 前端单页开发 | API -> Dashboard/H5 | 可先用演示数据，但不建议长期保持 |
| 后端接口开发 | DB/Redis -> API | 数据层应优先稳定 |
| Runtime 开发 | DB/Redis -> API -> Worker -> Runner Image | 需要完整运行底座 |
| 全链路联调 | DB/Redis/Object Storage -> API -> Worker -> Dashboard/H5 | 里程碑联调标准路径 |

## 13. 配置冻结点表

| 阶段 | 应冻结内容 | 冻结原因 |
|---|---|---|
| M1 结束前 | DB schema 基础主表结构 | 防止 API/前端反复返工 |
| M2 结束前 | runner image 启动路径、目录挂载协议 | 防止 worker/bridge 接口震荡 |
| M3 结束前 | credential/mcp 契约与策略字段 | 防止治理层和前端配置层反复调整 |
| M4 结束前 | dashboard/mobile 的 API SDK 主要域接口 | 保证联调稳定 |

## 14. 部署验证表

| 维度 | 检查项 | 期望结果 |
|---|---|---|
| API | `/health` | 正常 |
| Worker | 队列消费 | 正常接单 |
| Runtime | 真 run 启动 | 成功创建容器 |
| Dashboard | 工坊/实例页打开 | 正常 |
| Mobile H5 | 工坊/任务页打开 | 正常 |
| Files | 下载与读取 | 正常 |
| Auth | 登录与权限 | 正常 |
| Audit | 关键动作审计 | 正常 |

## 15. 备份与恢复矩阵表

| 对象 | 是否必须备份 | 建议策略 | 当前状态 |
|---|---|---|---|
| PostgreSQL | 是 | 定时全量 + 增量/WAL | 未建立 |
| Object Storage | 是 | Bucket 级备份/版本化 | 未建立 |
| Runner Image Tag | 是 | 镜像仓库存档 | 未建立 |
| CI/CD 配置 | 是 | Git 仓库存档 | 部分天然具备 |
| Runtime 临时目录 | 否 | 仅保留调试窗口 | 未建立 |
| 本地 runRoot | 否 | 可清理 | 当前开发态存在 |

## 16. 回滚矩阵表

| 层 | 回滚对象 | 回滚方式 | 当前状态 |
|---|---|---|---|
| Frontend | Dashboard/H5 静态资源 | 切换上一版本产物 | 未建立 |
| API | API 容器/进程版本 | 切换上一镜像/产物 | 未建立 |
| Worker | worker 版本 | 切换上一镜像/产物 | 未建立 |
| DB | schema 迁移 | 迁移回滚/恢复备份 | 未建立 |
| Runner Image | 运行镜像 tag | 回退到上一 tag | 未建立 |

## 17. 发布窗口表

| 环境 | 发布窗口建议 | 原因 |
|---|---|---|
| Shared Dev | 工作日白天 | 便于多人联调 |
| Staging | 工作日白天与傍晚 | 便于 QA/产品验证 |
| Production | 低峰窗口 | 降低用户影响 |

## 18. 运维职责矩阵表

| 事项 | 主负责角色 | 协同角色 |
|---|---|---|
| DB 运维 | Backend / Platform | Architect |
| Redis 运维 | Platform | Runtime |
| Object Storage 运维 | Platform | Backend |
| API 部署 | Backend | Platform |
| Worker / Runtime 部署 | Runtime | Platform |
| 镜像发布 | Platform | Runtime |
| 生产回滚 | Platform | Backend / Product |

## 19. 环境就绪度打勾表

| 项目 | Local Dev | Shared Dev | Staging | Production |
|---|---|---|---|---|
| DB | 待完成 | 待完成 | 待完成 | 待完成 |
| Redis | 待完成 | 待完成 | 待完成 | 待完成 |
| Object Storage | 待完成 | 待完成 | 待完成 | 待完成 |
| API | 已完成构建验证，未启动验证 | 未完成 | 未完成 | 未完成 |
| Worker | 已完成构建验证，未接队列 | 未完成 | 未完成 | 未完成 |
| Container Bridge | 已完成构建验证，未容器内验证 | 未完成 | 未完成 | 未完成 |
| Runner Image | 半成品，未本机运行验证 | 未完成 | 未完成 | 未完成 |
| Dashboard | 已完成构建验证 | 未完成 | 未完成 | 未完成 |
| Mobile H5 | 已完成构建验证 | 未完成 | 未完成 | 未完成 |

## 20. 部署拓扑分层表

| 分层 | 组件 | 主要职责 | 当前状态 | 目标形态 |
|---|---|---|---|---|
| 接入层 | Dashboard、Mobile H5 | 用户访问入口、会话展示、工坊浏览、任务交互 | 可构建原型工程 | CDN/静态托管 + 正式域名 |
| API 层 | `app/api` | 鉴权、工作区边界、run 聚合、文件与审批接口、realtime 出口 | 基础服务已成型 | 多实例可扩展服务 |
| 编排层 | `app/run-worker` | 创建 run 工作区、生成 runtime 物料、投递执行命令 | 物料生成已具备 | Redis/BullMQ 常驻 worker |
| 执行层 | Runner Image、Container Bridge、Codex CLI | 单 run 隔离执行、会话管理、文件监听、事件回传 | 半成品 | 每 run 独立容器执行 |
| 数据层 | PostgreSQL、Redis、Object Storage | 权威数据、队列、文件产物、缓存 | 未正式建立 | 托管或自管正式服务 |
| 治理层 | Secret Store、Observability、Audit | 密钥管理、日志指标、审计追踪、告警 | 未建立 | 平台级能力 |

## 21. Run 容器挂载与注入矩阵表

| 注入项 | 容器内目标路径/方式 | 来源 | 读写属性 | 用途 | 敏感级别 |
|---|---|---|---|---|---|
| Target Path | `/workspace` | 宿主 runRoot 工作目录 | 读写 | Codex CLI 主工作路径 | 中 |
| Inputs | `/workspace/inputs` | 宿主 inputs 目录 | 只读或受控读写 | 输入材料访问 | 中 |
| Outputs | `/workspace/outputs` | 宿主 outputs 目录 | 读写 | 产物输出 | 中 |
| Runtime Config | `/runtime/runtime-config.json` | worker 生成文件 | 只读 | 运行时主配置 | 中 |
| Bridge Context | `/runtime/bridge-context.container.json` | worker 生成文件 | 只读 | bridge 启动上下文 | 中 |
| MCP Config | `/runtime/mcp-config.json` | worker 生成文件 | 只读 | MCP server 配置 | 高 |
| Secret Manifest | `/runtime/secret-manifest.json` | worker 生成文件 | 只读 | secret 映射说明 | 高 |
| 环境变量 Secret | 进程环境变量 | secret broker / env 注入 | 只读 | API key、token、连接串 | 高 |
| 文件型 Secret | `/run/secrets/*` | secret broker / 文件挂载 | 只读 | 证书、配置、MCP 凭证 | 高 |

## 22. 第三方能力接入矩阵表

| 能力类型 | 接入对象 | 当前支持状态 | 部署侧要求 | 风险关注点 |
|---|---|---|---|---|
| 模型 API | OpenAI / 兼容网关 / 图像生成服务 | 设计明确，正式链未补齐 | secret 注入、出网许可、速率限制 | 费用、限流、秘钥泄露 |
| 第一方 MCP | 平台内自管 MCP | 可按 runtime config 规划 | 固定镜像、固定配置、审计 | 版本同步 |
| 第三方 MCP | 用户自带或外部不受控 MCP | 仅基础抽象已存在 | 独立凭证、网络边界、可观测性 | 越权、不可控出网、稳定性 |
| 浏览器自动化 | Playwright / Chrome 能力 | image 与依赖规划存在 | runner image 带浏览器依赖 | 资源占用、外部站点行为变化 |
| 文件下载/上传 | 对象存储 / API stream | 局部骨架已存在 | 对象存储、签名 URL | 大文件、权限、回源 |

## 23. 环境权限边界表

| 环境 | 可访问数据 | 可访问基础设施 | 权限原则 |
|---|---|---|---|
| Local Dev | 本地测试数据 | 本地自建 DB/Redis/MinIO | 单人自管，不承载真实生产数据 |
| Shared Dev | 脱敏开发数据或临时测试数据 | 共享 DB/Redis/对象存储 | 最小权限，便于多人联调 |
| Staging | 接近生产的非生产数据 | staging 全套依赖 | 用于验收，不允许开发者随意改配置 |
| Production | 正式权威数据 | production 全套依赖 | 最小权限、审批变更、全审计 |

## 24. 发布策略矩阵表

| 组件 | 建议发布策略 | 原因 | 当前准备度 |
|---|---|---|---|
| Dashboard | 静态资源版本化发布 | 可快速回退、兼容 CDN | 低 |
| Mobile H5 | 静态资源版本化发布 | 与 Dashboard 一致，便于回滚 | 低 |
| API | 滚动发布或蓝绿发布 | 避免中断实时链路 | 低 |
| Run Worker | 滚动发布 + 队列 drain | 防止正在处理的任务丢失 | 低 |
| Runner Image | tag 固化 + 灰度切换 | 不同 run 可绑定不同镜像版本 | 低 |
| DB Migration | 先兼容后切换 | 降低 schema 变更风险 | 低 |

## 25. 环境建设优先顺序表

| 优先级 | 建设项 | 目标结果 | 依赖 |
|---|---|---|---|
| P0 | 本地 DB/Redis/Object Storage 标准化启动方式 | 每位开发者可稳定拉起完整底座 | `infra/docker` compose / env / Dockerfile 已落地，待实机联调 |
| P0 | Shared Dev 共享环境 | 支撑多人联调与第一轮真链路验证 | 基础设施账户、部署脚本 |
| P0 | Runner Image 构建与分发链 | worker 可真正拉起容器执行 | `infra/docker`、镜像仓库 |
| P1 | Staging 验收环境 | 形成里程碑级验收场地 | Shared Dev 稳定后升级 |
| P1 | Observability 平台接入 | 发布后可观测、可告警、可定位 | API/worker 指标埋点 |
| P2 | 生产环境发布治理 | 支撑真实用户上线 | 前述链路全部补齐 |

## 26. 当前已验证环境基线表

| 检查项 | 当前结果 | 证据 |
|---|---|---|
| Node.js | 已安装，可用 | `node -v`=`v22.14.0` |
| pnpm | 已安装，可用 | `pnpm -v`=`11.7.0` |
| Docker Client | 已安装 | `docker version` 返回 Client 信息 |
| Docker Daemon | 未启动 | `docker version` 报错 `docker daemon is not running` |
| Shared Packages 构建 | 已通过 | `pnpm build:shared` |
| Backend 聚合构建 | 已通过 | `pnpm build:backend` |
| API 单仓构建 | 已通过 | `pnpm -C app/api build` |
| Run Worker 单仓构建 | 已通过 | `pnpm -C app/run-worker build` |
| Container Bridge 单仓构建 | 已通过 | `pnpm -C app/container-bridge build` |
| Dashboard 构建 | 已通过 | `pnpm -C app/dashboard build` |
| Mobile H5 构建 | 已通过 | `pnpm -C app/mobile build:h5` |
| Shared Dev 环境 | 未建立 | 当前无共享环境脚本、无部署记录 |
| Staging 环境 | 未建立 | 当前无部署记录 |
| Production 环境 | 未建立 | 当前无部署记录 |

## 27. 当前明确不可推进的部署项表

| 部署项 | 当前阻塞 | 直接影响 | 处理优先级 |
|---|---|---|---|
| 本机真容器联调 | Docker daemon 未启动 | 无法验证 worker -> runner image -> bridge 真执行链 | P0 |
| Shared Dev 部署 | DB/Redis/Object Storage 未标准化 | 无法进行多人联调 | P0 |
| Staging 验收 | 无 Runner Image 分发链、无正式环境配置 | 无法形成里程碑验收场地 | P0 |
| Production 发布 | 无 auth、audit、CI/CD、回滚链 | 无法进入正式上线准备 | P0 |
| 文件正式下载链 | 无对象存储与签名 URL | 文件与产物无法进入正式交付链 | P1 |
| 多租户权限验证 | 无正式工作区鉴权 | 无法验证企业用户隔离 | P1 |

## 28. 当前最小环境落地顺序表

| 顺位 | 建设项 | 完成判定 | 对后续解锁价值 |
|---|---|---|---|
| 1 | 本地 Docker daemon + runner image 本机启动验证 | 可在本机执行单 run 容器链 | 解锁 runtime 真执行证据 |
| 2 | 本地 DB/Redis/Object Storage 标准化脚本 | 一键拉起完整底座 | 解锁 API/worker 正式联调 |
| 3 | Shared Dev 共享环境 | 多人可访问 API、Worker、对象存储 | 解锁第一轮真实联调 |
| 4 | Runner Image Registry | 镜像可构建、push、pull、回滚 | 解锁云端执行发布 |
| 5 | Staging 环境 | 全链路验收可重复执行 | 解锁里程碑 Gate |
| 6 | Production 环境 | 具备发布、回滚、审计能力 | 解锁真实用户上线 |

## 29. 环境证据与责任归属表

| 领域 | 当前证据来源 | 主责任角色 | 下一步动作 |
|---|---|---|---|
| 本地工具链 | `node -v`、`pnpm -v`、`docker version` | Platform / 开发者本人 | 固化本地环境要求 |
| 应用构建 | 各仓 `pnpm build` 命令 | Frontend / Backend / Runtime | 接入 CI 重复验证 |
| 运行时链路 | `app/run-worker`、`app/container-bridge`、`infra/docker` 代码与构建产物 | Runtime | 补真容器执行验证 |
| 数据底座 | 当前文档与代码扫描 | Backend / Platform | 先建 DB/Redis/Object Storage 基础设施 |
| 共享环境 | 当前无证据 | Platform | 新建 Shared Dev 与部署脚本 |

# 灵办词元 系统实现差距审计

## 1. 文档信息

| 字段 | 内容 |
|---|---|
| 文档名称 | 系统实现差距审计 |
| 审计日期 | 2026-07-12 |
| 审计范围 | `agent-workshop` 根工作区、`app/*`、`packages/*`、`infra/*`、`tests/*`、核心产品与研发文档 |
| 审计目标 | 基于当前代码、当前文档、当前测试、当前环境，判断距离文档约束下的最终交付成品还有多远 |
| 结论口径 | 以本轮实际代码、目录、Git 状态、命令执行结果为准 |

## 2. 本轮审计输入

### 2.1 复查文档

- `docs/产品需求草案.md`
- `docs/前端需求.md`
- `docs/后端设计文档.md`
- `docs/后端开发文档.md`
- `docs/联调与验收清单.md`
- `docs/里程碑Gate打分表.md`
- `docs/工程状态总表.md`
- `docs/仓库完成度与模块状态表.md`
- `docs/最终交付差距与开发项-2026-07-12.md`

### 2.2 本轮实际核查代码面

| 维度 | 实际核查对象 |
|---|---|
| 五个应用仓 | `app/api`、`app/run-worker`、`app/container-bridge`、`app/dashboard`、`app/mobile` |
| 共享包 | `packages/db`、`packages/files`、`packages/session-pack`、`packages/contracts`、`packages/api-sdk`、`packages/realtime`、`packages/shared`、`packages/ui-tokens` |
| 前端 E2E | `tests/e2e/specs/dashboard.spec.mjs`、`tests/e2e/specs/mobile.spec.mjs` |
| CI | `.github/workflows/ci.yml` |
| 基础设施 | 根 `package.json`、`infra/docker/*`、`infra/scripts/local-stack.mjs` |

### 2.3 本轮命令证据

| 命令 | 结果 | 说明 |
|---|---|---|
| 本地 `docker / wsl` 验证 | 不执行（受约束） | 当前明确禁止调度任何本地虚拟化环境，因此本轮不以本地 Docker / WSL 作为验收基线 |
| `pnpm ci:verify` | 通过 | backend、runtime、frontend 全链基础回归通过 |
| `pnpm ci:backend` | 通过 | 共享包、API 构建与 smoke 通过 |
| `pnpm -C packages/files test:smoke` | 通过 | `packages/files` 当前 `3/3` |
| `pnpm ci:runtime` | 通过 | Worker / Bridge 测试通过 |
| `pnpm ci:frontend` | 通过 | Dashboard lint、Dashboard build、Mobile H5 build、Playwright `23/23` 全通过 |

## 3. 当前总体判断

本轮复查后，整体结论没有发生方向性变化。

| 维度 | 当前判断 | 说明 |
|---|---:|---|
| 代码主干完成度 | 84% | 主仓、共享包、正式域、测试骨架已成型 |
| 产品闭环完成度 | 64% | 工坊分发、任务对话、文件、Creator、Session API 已成型 |
| 最终交付就绪度 | 43% | 真容器、真 Codex、真第三方 MCP、独立交付、上线链仍未闭环 |

### 3.1 与最终交付的真实距离

| 判断问题 | 当前答案 |
|---|---|
| 系统架构是否已成立 | 是 |
| 主功能是否大部分已开工并进入正式工程 | 是 |
| 文档承诺的核心产品能力是否已闭环 | 否 |
| 是否可以宣称“开发基本结束” | 否 |
| 是否可以按企业级成品交付 | 否 |

## 4. 当前已经成立的部分

| 域 | 当前已成立事实 |
|---|---|
| API 正式域 | `auth / workspaces / workshops / creator / releases / runs / files / realtime / credentials / mcps / quotas / billing / me / notifications / search / batch-runs / sessions` 已进入正式代码与 smoke |
| Session API | `list / detail / import / archive / inherit / publish / rollback / unpublish / lineage` 已落地 |
| Session archive | session archive 字节已进入 `objectStore`，元数据索引已支持 `file/postgres` 双轨 |
| Run Worker | runtime 物料、session-pack 下载/解包、`workspace-base` 恢复、队列、cleanup、recovery 已落地 |
| Container Bridge | PTY、Codex session forwarding、artifact 回流、MCP materializer、remote MCP proxy 已落地 |
| Dashboard | 工坊、实例、Creator、治理主壳成立，实例页已支持完整对话工作面，Creator Session 面板已接正式写链 |
| Mobile H5 | 工坊、任务、文件、我的主链成立，任务页为完整对话模式，具备 outbox / ack / retry / restore |
| 自动化验证 | `packages/db` `25/25`、`packages/files` `3/3`、`packages/session-pack` `18/18`、`packages/api-sdk` `24/24`、`app/api` `57/57`、`app/run-worker` `27/27`、`app/container-bridge` `23/23`、`pnpm ci:frontend` 与 Playwright `23/23` 已有完整证据 |

## 5. 当前还没有成立的部分

### 5.1 最高优先级差距

| 编号 | 差距 | 当前状态 | 为什么仍然不能视为完成 |
|---|---|---|---|
| P0-01 | `session-pack` 生产闭环 | 已完成 pack/archive/import/export/inherit/publish/rollback/unpublish/lineage、slot schema、redaction map schema、文件级 SHA-256 完整性校验，以及 canonical manifest signature 生成/验真并接入 API import/export/inherit/internal fallback 链路；run `informationCollection` 已正式持久化结构化 `slotValues` 与 `answers`，review 摘要也已纳入 runtime-derived session-pack 导出、`/v1/sessions/:sessionVersionId` 详情与 Dashboard Creator `Session` 面板，并已补齐答案来源追踪、人工修订链与当前生效版本标识，consumer governance 摘要也已纳入 session detail 与 Creator 清单展示，session governance summary（状态 / 风险 / 下线风险 / 基线差异字段 / 派生计数）、signature governance summary（算法 / key / 验签状态 / 兼容性诊断）与 policy summary（动作级放行 / 警告 / 阻断、脱敏复核门禁、覆盖完整性门禁、验签门禁）也已纳入正式 detail、SDK、Creator 页面与 smoke/E2E；后端已对 redacted export / publish / inherit / rollback / unpublish 接入统一策略评估，并在命中阻断时返回正式治理错误；redaction coverage、preview 命中摘要、redaction review 记录、archive export audit 与手工敏感位补标也已纳入 session detail 与 Creator 可视化复核，`POST /v1/sessions/:sessionVersionId/redaction-map`、`POST /v1/sessions/:sessionVersionId/redaction-review`、SDK `updateSessionPackRedactionMap(...) / reviewSessionPackRedaction(...)` 与 Dashboard Creator redaction rule draft/edit/remove/save + secret-slot curation + review 决策写链也已落地，internal run archive 已改为按 run 精确导出；archive 导出链已支持 `GET /v1/sessions/:sessionVersionId/archive?redact=true`，SDK 已支持 `downloadSessionPackArchive(..., { redact: true })`，Dashboard Creator 已补齐“下载脱敏归档”；双前端已可直接复核结构化答案；当前已补齐多 key 验签窗口、签名 key 分发目标摘要与 publish / rollback 动作级轮换治理阻断，以及 `runtime-evidence.json` / `runtimeEvidenceSummary` / runtime evidence 元数据索引落盘 / Creator runtime evidence 卡片 | 代码层 formal runtime evidence 已落地，缺远端隔离运行时 live consumption 实机证据 |
| P0-02 | 真 Docker per-run 执行 | 代码路径已在 | 当前明确禁止调度本地 `docker / wsl`，且尚无替代的远端容器验收环境，因此没有真容器实机证据 |
| P0-03 | 真容器内 Codex CLI 联调 | 代码路径已在 | 当前明确禁止调度本地 `docker / wsl`，且尚无替代的远端容器验收环境，因此缺容器内长会话、恢复、异常退出、文件回流实机证据 |
| P0-04 | 真第三方 MCP 联调 | 有治理代码与测试桩 | 缺不受控第三方 MCP 的真实探活、超时、熔断、审计、网络策略证据 |
| P0-05 | 双前端业务级 E2E | 已覆盖启动 -> 首轮信息收集 -> 审批 / 结构化答案复核的 smoke 骨架，Dashboard Creator 也已补齐 publish / inherit / rollback 业务主链，Dashboard / Mobile `plain-text / office-text / image / pdf / download-only` 文件预览下载矩阵、附件上传/发送主链，以及 Dashboard 批量运行生命周期主链也已纳入回归 | 缺真实后端/runtime/files 参与的全链 E2E |
| P0-06 | 多仓独立交付 | 五仓仍依赖 `workspace:*` | 五个远端仓库脱离根工作区后仍不能独立安装、构建、测试、发布 |
| P0-07 | 本地完整联调栈 | compose 与脚本已在 | 当前明确禁止调度本地 `docker / wsl`，因此不能以本地虚拟化环境补齐 PostgreSQL / Redis / MinIO / API / Worker / Dashboard / H5 全栈实机证据 |
| P0-08 | 发布、回滚、上线 Gate | 只有 CI build/test | 没有镜像发布、环境切换、DB migration gate、回滚剧本、上线检查链 |
| P0-09 | 正式共享数据层收敛 | `packages/db` 已建立 auth/creator/runs/event/run-files/uploads/query/bridge/billing/notifications/me-recent/me-favorites/batch-runs/search/session-archives/quotas/workshops/credentials/mcp 共享骨架，并已具备 seed/reset CLI | 深度 query model、更系统化 read-model 收敛与 seed 数据治理仍未完全切换到正式共享数据层 |

### 5.2 中高优先级差距

| 编号 | 差距 | 当前状态 |
|---|---|---|
| P1-01 | `packages/files` 深化 | 已落地最小共享层，当前承接 path boundary、logical path、mime/preview 推断与 Office 文本抽取 |
| P1-02 | `packages/realtime` | 已落地前端 run stream 共享包，当前只覆盖客户端 hook 层 |
| P1-03 | `packages/shared` | 已落地最小共享层，当前承接 `toErrorMessage / nowIso / buildAtomicTempPath` |
| P1-04 | Dashboard 深治理能力 | 仍缺更深策略化 consumer 治理、lineage 可视化、批量治理 |
| P1-05 | Batch 产品化深化 | 当前只有批次草稿、导入解析、预算预估、基础看板 |
| P1-06 | 双前端统一组件层 | 仍有大量页面内联实现 |
| P1-07 | Mobile 平台特化 | 首发仅 H5，`weapp / alipay` 仍未特化 |
| P1-08 | 完整文件预览矩阵 | `plain-text / office-text / image / pdf / download-only` smoke 已补齐，剩余缺口收敛到更广 Office 家族特化、跨页恢复与批量下载体验 |
| P1-09 | 监控与告警 | 代码级基础存在，生产级链路未建立 |
| P1-10 | 埋点与漏斗 | 双前端与 API 尚未形成完整行为分析链 |

## 6. 本轮新增确认的现实差异

### 6.1 双前端“启动 -> 首轮信息收集 -> 审批 / 答案复核”链已补齐到 smoke 级验收

本轮代码复查确认：

- [runStream.ts](C:/dev/copypaste/agent-workshop/app/dashboard/src/lib/runStream.ts) 已支持 `approve` / `approveAwaitAck`
- [InstancesPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/instances/InstancesPage.tsx) 已显性展示 `informationCollection` 摘要、缺口统计与继续引导动作
- [InstancesPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/instances/InstancesPage.tsx) 已落地 pending approval 内联卡片与审批动作
- [InstancesPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/instances/InstancesPage.tsx) 已落地结构化答案复核面板、通过/驳回/改写动作
- [WorkshopsPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/workshops/WorkshopsPage.tsx) 已补 launch 按钮测试锚点并支持从服务启动进入实例对话页
- [prototype.css](C:/dev/copypaste/agent-workshop/app/dashboard/src/styles/prototype.css) 已补齐 `approval-inline-card / approval-action-row / approval-inline-note`
- [dashboard.spec.mjs](C:/dev/copypaste/agent-workshop/tests/e2e/specs/dashboard.spec.mjs) 已补齐 Dashboard 启动 -> 首轮信息收集 -> 审批 / 结构化答案复核用例，并继续补齐实例文件预览 + 下载、实例附件上传/发送、Creator `session-pack` 敏感位补标、脱敏复核导出审计与 publish / inherit / rollback 用例且已通过
- [detail.tsx](C:/dev/copypaste/agent-workshop/app/mobile/src/pages/tasks/detail.tsx) 已显性展示 `informationCollection` 摘要、缺口统计与继续引导动作
- [detail.tsx](C:/dev/copypaste/agent-workshop/app/mobile/src/pages/tasks/detail.tsx) 已补齐移动端审批测试锚点
- [detail.tsx](C:/dev/copypaste/agent-workshop/app/mobile/src/pages/tasks/detail.tsx) 已落地移动端结构化答案复核面板、通过/驳回/改写动作
- [services/detail.tsx](C:/dev/copypaste/agent-workshop/app/mobile/src/pages/services/detail.tsx) 与 [index.tsx](C:/dev/copypaste/agent-workshop/app/mobile/src/pages/workshops/index.tsx) 已补移动端服务启动测试锚点
- [mobile.spec.mjs](C:/dev/copypaste/agent-workshop/tests/e2e/specs/mobile.spec.mjs) 已补齐 Mobile H5 启动 -> 首轮信息收集 -> 审批 / 结构化答案复核用例并通过

结论：双前端已经从“页面实现”推进到“服务启动 -> 首轮信息收集 -> 审批 / 结构化答案复核”的 smoke 级自动化验收，Dashboard / Mobile `plain-text / office-text / image / pdf / download-only` 文件预览下载矩阵、附件上传/发送主链、Dashboard 批量运行生命周期主链，以及 Dashboard Creator `session-pack` 的脱敏复核导出审计与 publish / inherit / rollback 也已进入业务级回刷。当前仍缺真实后端、真实 runtime、真实文件与真实审批回流参与的业务级 E2E。

### 6.2 双前端静态工作区/目录数据已明显收缩，但尚未完全退场

代码证据：

- [workspaceContext.ts](C:/dev/copypaste/agent-workshop/app/dashboard/src/lib/workspaceContext.ts) 已不再依赖 `dashboardWorkspaces`，预览态只保留最小推断工作区视图
- [workspaceContext.ts](C:/dev/copypaste/agent-workshop/app/mobile/src/lib/workspaceContext.ts) 已不再依赖 `workspaceEntries`，移动端工作区列表在预览态收敛为当前工作区
- [workspaceCatalog.ts](C:/dev/copypaste/agent-workshop/app/mobile/src/data/workspaceCatalog.ts) 已删除，本地可见性白名单已退出代码树
- [WorkshopsPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/workshops/WorkshopsPage.tsx) 不再在 runs 为空时回填静态 `instances`
- [InstancesPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/instances/InstancesPage.tsx) 不再依赖静态 `dashboardServices` 做实例服务推断

结论：runs 主链、工作区解析层、样例实例回填和移动端本地可见性白名单已经进一步收敛；剩余静态层主要集中在 `dashboardData.ts` / `mobileData.ts` 中尚未删掉的展示参考对象、预览态文案与部分 Creator 参考数据，距离“目录域完全真数据化”还有一段距离。

### 6.3 CI 只有构建与测试，没有交付链

代码证据：

- 当前 `.github/workflows` 下仅有 [ci.yml](C:/dev/copypaste/agent-workshop/.github/workflows/ci.yml)

结论：

- 当前只有 `backend-smoke / runtime-tests / frontend-e2e`
- 没有镜像发布
- 没有环境部署
- 没有 DB migration gate
- 没有回滚作业
- 没有 release promotion 机制

### 6.4 `session-pack` manifest signature 链已从设计缺口转为已落地能力

本轮代码与验证确认：

- [signature.ts](C:/dev/copypaste/agent-workshop/packages/session-pack/src/signature.ts) 已落地 canonical manifest signature 的生成、签名与验真，支持 `sha256`、`hmac-sha256`、`ed25519`
- [pack.ts](C:/dev/copypaste/agent-workshop/packages/session-pack/src/pack.ts) 已支持打包阶段直接生成签名 manifest
- [service.ts](C:/dev/copypaste/agent-workshop/app/api/src/modules/sessions/service.ts) 已把签名策略接入 imported/runtime-derived/generated/inherit export 链，并在导入时执行签名校验
- [routes.ts](C:/dev/copypaste/agent-workshop/app/api/src/modules/bridge/routes.ts) 已把 internal runtime-fallback 导出的 session-pack 接入同一签名策略
- [session-pack.test.mjs](C:/dev/copypaste/agent-workshop/packages/session-pack/tests/session-pack.test.mjs) 已补齐 `sha256`、`hmac-sha256`、`ed25519` 覆盖
- [session-pack-archive.smoke.test.mjs](C:/dev/copypaste/agent-workshop/app/api/tests/session-pack-archive.smoke.test.mjs) 已验证 import/export/inherit/runtime-derived/runtime-fallback/generate 链路上的签名行为

结论：`manifest signature 生成/验真缺失` 已经不再是当前差距表中的成立项，Creator 侧签名可视化与兼容性诊断也已进入正式主链，多 key 验签窗口与签名 key 分发治理阻断也已接入动作主链。当前剩余缺口转移为 live runtime consumption proof。

### 6.5 `slot answer` 已从摘要态升级为结构化持久化

本轮代码与验证确认：

- [runs.ts](C:/dev/copypaste/agent-workshop/packages/contracts/src/runs.ts) 已把 `runConversationMessage.slotValues` 与 `runInformationCollection.answers` 收口到正式契约
- [runs.ts](C:/dev/copypaste/agent-workshop/packages/domain-models/src/runs.ts) 已支持显式槽位值、自然语言推断槽位值与附件绑定的结构化答案生成
- [service.ts](C:/dev/copypaste/agent-workshop/app/api/src/modules/runs/service.ts) 已在消息写入时持久化解析后的 `slotValues`，并把结构化答案写回 `informationCollection.answers`
- [run-information-collection.smoke.test.mjs](C:/dev/copypaste/agent-workshop/app/api/tests/run-information-collection.smoke.test.mjs) 已覆盖附件槽位、推断文本槽位、显式结构化文本槽位三类路径
- `pnpm ci:backend` 本轮已通过，说明契约、领域层、API 与 smoke 已形成一致验证

结论：`slot answer 结构化持久化缺失` 已经不再是当前差距表中的成立项。当前剩余缺口转移为 runtime consumption proof。

### 6.6 `consumerGovernance` 摘要链已补齐到正式消费面

本轮代码与验证确认：

- [sessions.ts](C:/dev/copypaste/agent-workshop/packages/contracts/src/sessions.ts) 已把 `consumerGovernance` 收口到正式 session detail 契约
- [service.ts](C:/dev/copypaste/agent-workshop/app/api/src/modules/sessions/service.ts) 已基于 lineage 可见下游版本构建 `consumerGovernance` 摘要，并把当前版本 / 下游消费版本统一投影到 `/v1/sessions/:sessionVersionId`
- [CreatorPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/creator/CreatorPage.tsx) 已补齐 `consumer governance map` 检索与清单展示，可直接查看版本、实例与文件入口
- [sessions.ts](C:/dev/copypaste/agent-workshop/packages/contracts/src/sessions.ts) 已新增正式 `governanceSummary` 契约，覆盖状态、风险、下线风险、基线差异字段与派生计数
- [service.ts](C:/dev/copypaste/agent-workshop/app/api/src/modules/sessions/service.ts) 已基于 lineage / published targets / consumer versions / runtime evidence 计算 `governanceSummary`
- [service.ts](C:/dev/copypaste/agent-workshop/app/api/src/modules/sessions/service.ts) 已新增 `runtime-evidence.json` 生成、归档索引持久化与 `runtimeEvidenceSummary` 聚合
- [CreatorPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/creator/CreatorPage.tsx) 已新增 Creator `runtime evidence` 可视化卡片与 run/files/runtime 跳转
- [dashboard.spec.mjs](C:/dev/copypaste/agent-workshop/tests/e2e/specs/dashboard.spec.mjs) 已补齐 Creator `session governance summary` 页面冒烟断言
- [sessions.ts](C:/dev/copypaste/agent-workshop/packages/contracts/src/sessions.ts) 已新增正式 `signatureSummary` 契约，覆盖算法、key、验签状态、兼容性与诊断信息
- [service.ts](C:/dev/copypaste/agent-workshop/app/api/src/modules/sessions/service.ts) 已基于 manifest signature 与运行时签名配置计算 `signatureSummary`
- [CreatorPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/creator/CreatorPage.tsx) 已补齐 Creator `signature governance` 可视化卡片
- [session-pack-archive.smoke.test.mjs](C:/dev/copypaste/agent-workshop/app/api/tests/session-pack-archive.smoke.test.mjs) 已验证基础版本与 consumer 版本都能返回正确的 `consumerGovernance` 摘要
- [sessions-client.test.mjs](C:/dev/copypaste/agent-workshop/packages/api-sdk/tests/sessions-client.test.mjs) 已验证 SDK 可正确解析 `consumerGovernance`

结论：`consumerGovernance` 摘要链已经从“首层卡片”推进到正式 detail / SDK / Creator 清单主链，`governanceSummary` 已把停用/下线风险、基线差异审计与派生计数推进到正式消费主链，`signatureSummary` 已把签名算法 / key / 验签兼容性诊断、多 key 验签窗口与分发目标摘要推进到正式消费主链，`policySummary` 已把动作级阻断/告警推进到正式消费主链。当前剩余缺口转移为 live runtime consumption proof。

## 7. 仓库级差距清单

| 仓库 | 当前判断 | 已实现模块 | 未完成模块 | 当前最大缺口 |
|---|---|---|---|---|
| `app/api` | 最成熟 | 正式 API 域、session archive、batch-runs、治理域、billing/quota/me/notifications/search、run `informationCollection` 结构化答案持久化与 review 写链 | `packages/db` 深化、`informationCollection` 与 Creator / session-pack / 审计导出的治理闭环、真实对象存储与 live runtime 证据、部署链 | 代码很强，实机证据不足 |
| `app/run-worker` | 主干成立 | runtime 物料、session-pack materializer、Docker create/start/stop/rm 路径、recovery、cleanup、ops/diagnostics | 真 daemon、真 runner image、资源治理证据、跨节点补偿 | 真容器执行没有落证据 |
| `app/container-bridge` | 主干成立 | PTY、Codex session、artifact 回流、MCP materializer、remote proxy、runtime firewall | 真 Codex CLI、真第三方 MCP、browser profile 生命周期、跨重启恢复增强 | 缺 live 运行链证据 |
| `app/dashboard` | 主体成型 | 工坊/实例/Creator/治理主壳、Session 面板、批次基础能力、实例完整对话模式、`informationCollection` 显性展示与结构化答案复核、consumer governance 摘要 / 清单、session governance summary、signature governance summary、policy summary、启动/首轮信息收集/审批/答案复核 smoke E2E | 更深 lineage、批量治理、目录域去静态化、业务级 E2E | 页面强，验收与数据权威性不足 |
| `app/mobile` | 主体成型 | 工坊/任务/文件/我的、完整对话、outbox/ack/retry、workspace 切换、`informationCollection` 显性展示与结构化答案复核、启动/首轮信息收集/审批/答案复核 smoke E2E | `weapp/alipay`、附件跨页恢复增强、文件预览矩阵、目录域去静态化、业务级 E2E | 首发 H5 可展示，仍未达到正式交付 |

## 8. 共享层差距清单

| 包 | 当前状态 | 还要做什么 |
|---|---|---|
| `packages/db` | 已有最小正式骨架，已收口 auth/creator/runs/event/run-files/uploads/query/bridge/billing/notifications/me-recent/me-favorites/batch-runs/search/session-archives/quotas/workshops/credentials/mcp，并具备 seed/reset CLI | 扩展更多 query repository，并继续收敛更深 read-model、正式边界与 seed 数据治理 |
| `packages/session-pack` | 已有 pack/archive/workspace-base、slot schema、redaction map schema、文件级完整性校验与 manifest signature 生成/验真；run `informationCollection` 结构化 `slotValues` / `answers` 与 review 主链已收口到正式契约与领域层，review 摘要、consumer governance 摘要、session governance summary、signature governance summary、policy summary、redaction coverage / preview 命中摘要、redaction review 记录、archive export audit 与手工敏感位补标已纳入 runtime-derived session-pack 导出与消费展示主链，archive 导出链已支持 `?redact=true` 脱敏导出，且自动治理阻断、多 key 验签窗口与签名 key 分发治理已接入动作主链 | 远端 runtime consumption proof |
| `packages/files` | 已落地最小共享层 | 继续下沉对象存储、preview/download helper、ticket、path policy、retention |
| `packages/realtime` | 已有最小共享层 | 已收口 Dashboard / Mobile 的 run realtime hook；API 服务端协议、resume token、reconnect/backlog 与更完整客户端 SDK 仍待继续抽离 |
| `packages/shared` | 已落地最小共享层 | 继续抽离 logger、id、retry、telemetry、feature flag 与更多跨应用基础设施 |

## 9. 详细开发项清单

### 9.1 `session-pack` 资产化闭环

| 模块 | 具体开发项 |
|---|---|
| Redaction | 策略编排深化 |

### 6.7 `redactionSummary` 摘要链已补齐到正式消费面

本轮代码与验证确认：

- [sessions.ts](C:/dev/copypaste/agent-workshop/packages/contracts/src/sessions.ts) 已把 `redactionSummary` 收口到正式 `sessionPackDetail` 契约
- [service.ts](C:/dev/copypaste/agent-workshop/app/api/src/modules/sessions/service.ts) 已基于 `slot-schema.json` 与 `redaction-map.json` 解析 secret 槽位覆盖、孤儿 `slot_key`、规则类型与策略摘要
- [CreatorPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/creator/CreatorPage.tsx) 已补齐 Creator `Session` 面板的脱敏覆盖摘要、规则清单，以及 redaction rule draft/edit/remove/save 写链
- [session-pack-archive.smoke.test.mjs](C:/dev/copypaste/agent-workshop/app/api/tests/session-pack-archive.smoke.test.mjs) 已验证 detail 可返回 secret 槽位覆盖摘要
- [sessions-client.test.mjs](C:/dev/copypaste/agent-workshop/packages/api-sdk/tests/sessions-client.test.mjs) 已验证 SDK 可正确解析 `redactionSummary`

结论：`脱敏规则完全不可见` 与 `脱敏规则只能只读不可写` 已经不再是当前差距表中的成立项。当前剩余缺口转移为 runtime consumption proof。

### 6.8 `redaction preview` 命中摘要已补齐到正式消费面

本轮代码与验证确认：

- [sessions.ts](C:/dev/copypaste/agent-workshop/packages/contracts/src/sessions.ts) 已把 `previewMatchedRuleCount`、`previewTotalMatches`、`previewUnmatchedRuleIds` 与规则级命中字段收口进 `redactionSummary`
- [service.ts](C:/dev/copypaste/agent-workshop/app/api/src/modules/sessions/service.ts) 已在 detail 读取链中直接执行 redaction preview，计算命中次数、受影响文件与失败信息
- [CreatorPage.tsx](C:/dev/copypaste/agent-workshop/app/dashboard/src/pages/creator/CreatorPage.tsx) 已补齐 Creator `Session` 面板的预检命中展示与未命中规则提示
- [session-pack-archive.smoke.test.mjs](C:/dev/copypaste/agent-workshop/app/api/tests/session-pack-archive.smoke.test.mjs) 已验证 detail 返回 preview 摘要
- [sessions-client.test.mjs](C:/dev/copypaste/agent-workshop/packages/api-sdk/tests/sessions-client.test.mjs) 已验证 SDK 可正确解析 preview 字段

结论：`规则命中可视化与策略校验` 已经不再是当前差距表中的成立项。当前剩余缺口转移为 runtime consumption proof。
| Slot | 已有双前端 review、redaction review 记录、archive export audit、答案版本治理与 slot 填报来源追踪，继续补覆盖/修订策略冲突规则与自动化治理策略 |
| Signature | 远端消费证据、失败诊断标准化深化 |
| Runtime proof | pack 被哪个 run、哪个 worker、哪个 image 消费的证据链 |
| Creator 治理 | 更深批量治理、远端消费证据 |

### 9.2 真运行链

| 模块 | 具体开发项 |
|---|---|
| Docker | 启动 daemon、构建 runner image、容器启动/挂载/回收/inspect 证据 |
| Codex CLI | 长会话、文件写出、恢复、异常退出、控制命令、产物回流 |
| Bridge | 真实 control、真实 artifact watcher、真实 file watcher、恢复链演练 |
| Local stack | PostgreSQL、Redis、MinIO、API、Worker、Dashboard、H5 联调脚本与证据 |
| File chain | target path 浏览、preview、download、artifact 回流、对象存储回源 |

### 9.3 MCP 与凭证治理

| 模块 | 具体开发项 |
|---|---|
| First-party MCP | 真实容器运行验证 |
| Workspace-managed MCP | 真实策略、审批、网络边界验证 |
| Third-party MCP | 不受控远端探活、超时、熔断、断线、恢复、审计 |
| Credential injection | env/file/private secret 注入与回收证据 |
| Governance | provider callback、expiry freeze、rotation、usage audit、run 级冻结回流 |

### 9.4 双前端正式验收

| 端 | 具体开发项 |
|---|---|
| Dashboard | 登录、工作区切换、工坊启动、首轮追问、审批、文件下载、Creator 发布/回滚/继承、批量运行、治理审批 E2E |
| Mobile H5 | 登录、任务启动、任务对话、附件上传/发送、审批、文件预览/下载、工作区切换、我的页资产沉淀 E2E |
| 共同项 | 去静态目录、去静态工作区、统一错误态/空态/骨架态、统一组件层、行为埋点 |

### 9.5 交付与运维

| 模块 | 具体开发项 |
|---|---|
| Package publish | 解决 `workspace:*`，形成共享包发布与版本锁定策略 |
| Repo independence | 五个远端仓库脱离根工作区后独立安装、构建、测试、发布 |
| Deploy | 镜像发布、环境配置、部署流水线、迁移 gate |
| Rollback | 前端、后端、DB 回滚剧本与演练 |
| Observability | metrics、logs、trace、alert routing、故障定位证据 |

## 10. 阻塞项

| 阻塞项 | 当前事实 | 影响范围 |
|---|---|---|
| 本地虚拟化环境禁用 | 当前明确禁止调度任何本地 `docker / wsl` | 真容器、真 Codex、真第三方 MCP、前后端 live 联调 |
| 根工作区不是 Git 仓库 | 当前成立 | `docs / infra / packages / tests` 缺统一远端治理 |
| 五仓依赖 `workspace:*` | 当前成立 | 多仓独立交付无法成立 |
| 双前端仍有部分静态展示层与 preview fallback | 当前成立 | 真实产品闭环与验收口径受影响 |

## 11. 推进顺序建议

| 顺位 | 动作 | 目标 |
|---|---|---|
| 1 | 完成 `packages/db` 与 `session-pack` 两条共享主链收敛 | 固化长期事实源与资产化核心 |
| 2 | 打通远端隔离运行链 + 真 Codex CLI + 真第三方 MCP | 建立平台最核心的执行证据 |
| 3 | 清退双前端静态目录/工作区数据，补齐业务级 E2E | 建立可验收前台 |
| 4 | 解决多仓独立交付与共享包发布 | 具备真实工程交付条件 |
| 5 | 建立发布、回滚、监控、告警与上线 Gate | 进入正式上线准备阶段 |

## 12. 本轮审计结论

当前项目已经具备强工程样机形态，离“最终成品”还差最后一层最重的工程闭环：

1. 资产化闭环  
2. 真运行闭环  
3. 真验收闭环  
4. 真交付闭环  

当前不能按“开发基本结束”口径描述。

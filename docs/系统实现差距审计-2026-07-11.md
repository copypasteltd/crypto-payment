# 灵办词元 系统实现差距审计

## 1. 文档信息

| 字段 | 内容 |
|---|---|
| 文档名称 | 系统实现差距审计 |
| 审计日期 | 2026-07-11 |
| 审计范围 | `agent-workshop` 根工作区、`app/*` 五个独立仓库、`packages/*` 共享层、`infra/*`、`tests/*`、核心产品与研发文档 |
| 审计目标 | 复核当前实现与文档约束的偏差，给出当前完成度、剩余距离与具体开发项 |
| 审计口径 | 以当前代码、目录结构、Git 状态、命令执行结果、测试结果与文档正文为准 |

## 2. 本轮权威证据

| 类别 | 当前事实 |
|---|---|
| 根路径 | `C:\dev\copypaste\agent-workshop` |
| 根工作区 Git | 根路径本身不是 Git 仓库 |
| 独立仓库 | `app/api`、`app/dashboard`、`app/mobile`、`app/run-worker`、`app/container-bridge` |
| 当前提交 | `api=372bc23`、`dashboard=322b4e1`、`mobile=af0b2ae`、`run-worker=78b1de7`、`container-bridge=92876c8` |
| 当前脏仓库 | `app/api`、`app/dashboard`、`app/mobile`、`app/run-worker`、`app/container-bridge` 均存在未提交改动 |
| 已存在共享包 | `api-sdk`、`config`、`contracts`、`credential`、`db`、`domain-models`、`mcp`、`session-pack`、`ui-tokens` |
| 缺失共享包 | `packages/files`、`packages/realtime`、`packages/shared` |
| workspace 依赖现状 | 五个应用仓与多个共享包仍依赖 `workspace:*` |
| CI 验证链现状 | 根级 `build:backend` 已改为共享包单次构建后顺序执行 `app/container-bridge -> app/run-worker -> app/api` 本地编译，`ci:backend / ci:runtime / ci:verify` 已切换为 compiled-test 口径 |
| Docker 状态 | `docker version` 可返回 Client 信息，daemon 未运行 |

## 3. 本轮验证结果

| 验证项 | 结果 |
|---|---|
| `pnpm.cmd ci:verify` | 通过 |
| `pnpm.cmd -C packages/api-sdk test:smoke` | `22/22` 通过 |
| `pnpm.cmd -C app/run-worker test` | `27/27` 通过 |
| `pnpm.cmd -C app/container-bridge test` | `23/23` 通过 |
| `pnpm.cmd -C app/dashboard build` | 通过 |
| `pnpm.cmd -C app/mobile build:h5` | 通过 |
| `pnpm.cmd -C app/api test:smoke` | `57/57` 通过 |
| `pnpm.cmd test:e2e` | `dashboard 5 + mobile 4 = 9/9` 通过 |
| `pnpm.cmd -C app/api exec node --test --test-concurrency=1 tests/session-pack-archive.smoke.test.mjs` | 通过 |
| `pnpm.cmd -C app/api exec node --test --test-concurrency=1 tests/creator-launch-template-activation.smoke.test.mjs` | 通过 |
| `docker version` | daemon 不可用，真容器链未验证 |

## 4. 当前状态结论

| 维度 | 当前判断 | 说明 |
|---|---:|---|
| 代码主干完成度 | 82% | 五个应用仓与九个共享包已经形成正式工程主干 |
| 产品闭环完成度 | 64% | 工坊分发、实例运行、任务对话、文件回流、Creator 治理、Session API 主链已成型 |
| 最终交付就绪度 | 43% | 真容器执行、真 Codex CLI、真第三方 MCP、独立交付、部署回滚、业务级 E2E 仍未闭环 |

当前距离文档约束的最终成品，核心还差三条主链：

1. `session-pack` 从 Creator 资产生产到消费继承再到 runtime 消费的完整闭环。
2. `API -> worker -> Docker -> bridge -> Codex -> 文件回流 -> 双前端` 的真实运行证据链。
3. 五仓库脱离根工作区后的独立构建、部署、发布、回滚与观测链。

## 5. 文档已过期或失真项

| 文档主题 | 旧结论 | 当前事实 | 处理建议 |
|---|---|---|---|
| Session lineage | 仍有文档把 lineage 查询列为未落地 | `/v1/sessions/:sessionVersionId/lineage`、SDK 接口、Dashboard Creator lineage 面板均已存在 | 将“无 lineage API”改写为“lineage 已落地，仍缺 consumer inherit 与更深差异可视化” |
| Worker 消费 session-pack | 仍有文档写成“仅规划下载/解包” | worker 已下载 archive、解包 bundle、恢复 `workspace-base` 到 `targetPath`，并写入 `workspaceBaseRestore` 元数据 | 文档口径应调整为“已落地 staging/restore，缺容器内 live 消费证据” |
| `packages/db` 状态 | 仍有文档写成“最小 skeleton” | 已提供 manager、migration、transaction helper，以及 runs/event/run-files/query 的共享契约与 Postgres 实现 | 文档应提升为“正式共享数据层已起步，仍缺进一步收敛” |
| Session archive 存储 | 部分文档把当前态写成“仅有对象存储字节链，没有正式元数据仓储” | `app/api` 当前已将 session archive 字节写入 `objectStore`，元数据索引已支持 `file/postgres` 双轨，并保留 legacy 本地归档兼容读取 | 文档应改成“archive 字节已 objectStore 化，元数据索引已支持 file/postgres 双轨，剩余缺口转向生产切流与实机联调” |
| Dashboard 批量启动 | 部分文档或状态说明仍把批量能力写成“显式字段映射未落地” | Dashboard 已存在 `/dashboard/services/:serviceId/batches/:batchJobId` 路由、`WorkshopsPage` 批次草稿、CSV/XLSX 导入解析、工作表切换、显式字段映射与看板、API `/v1/batch-runs` 正式域与 PostgreSQL 持久化 | 文档应改成“基础批次域、导入解析、显式字段映射与正式持久化已落地，仍缺更深执行治理” |
| Dashboard Session 治理 | 部分文档把 Session 面板写成“已具备完整治理闭环” | Dashboard 当前已补齐 inherit draft / consumer-derived create / publish / unpublish / rollback / archive download，并新增 consumer 治理卡；更深 lineage 可视化与 live 消费证据仍未闭环 | 文档应改成“Session 主写链与首层治理面已齐，深治理仍未闭环” |
| API SDK 测试基线 | 部分文档停留在 `16/16` 或 `20/20` | 当前 smoke 为 `22/22` | 刷新测试基线 |
| API smoke 稳定性 | 历史文档存在旧快照 | 本轮 `57/57` 通过 | 以 2026-07-11 审计结果为准 |
| 根级 CI 递归构建 | 历史链路存在 `build:backend -> app/api test:smoke -> build:backend` 套嵌 | 当前已改为单次 build + compiled tests，后续文档不应再把它列为现行阻塞 |

## 6. 已确认完成的部分

| 域 | 已完成内容 |
|---|---|
| API 正式域 | `auth / workspaces / workshops / creator / releases / runs / files / realtime / credentials / mcps / quotas / billing / me / notifications / search` 均有正式代码与 smoke |
| Session API | 列表、详情、import、archive、inherit draft、publish、rollback、unpublish、lineage 已落地，正式 archive 已支持 `imported / runtime-derived / generated` 三层来源 |
| Session 消费继承后端主链 | contract 已支持 `draft / consumer` 两种 inherit mode，run create 已可自动派生 consumer session-pack |
| Worker | run workspace、runtime 物料、session-pack staging、workspace-base restore、queue、cleanup、recovery、ops/diagnostics 已落地 |
| Bridge | PTY、Codex session forwarding、artifact 回流、MCP materializer、remote MCP proxy、runtime egress/firewall 已落地 |
| Dashboard | 工坊、实例、Creator、治理、多语言主壳已成型，Creator Session 面板已接正式 session 接口，消费派生识别、lineage 与基线差异视图，以及首层 consumer 治理卡、archive/runtime 证据摘要与实例跳转已落地 |
| Mobile H5 | 工坊、服务详情、任务列表、任务对话、文件页、我的页与工作区切换已成型 |
| 基础测试链 | backend/runtime/frontend 均具备正式构建与 smoke/E2E baseline |
| 根级 CI 收敛 | `ci:backend / ci:runtime / ci:verify` 已切换为编译后测试口径，根级验收不再重复触发子仓递归构建 |

## 7. 当前未完成项总表

### 7.1 P0 必做

| 编号 | 开发项 | 当前状态 | 具体开发内容 |
|---|---|---|---|
| P0-01 | `session-pack` 生产闭环 | 已完成 archive、import/export、inherit draft、publish、rollback、unpublish、lineage 基础链，且正式 archive 已优先读取 imported 与 matching run 的 runtime-derived snapshot | 补齐脱敏执行引擎、slot 抽取、人工复核、签名、容器内 live 消费证据 |
| P0-02 | Dashboard Session 治理闭环 | 后端主链已落地，Dashboard Creator Session 面板已补消费派生识别、lineage 标签、consumer-derived create、`unpublish` 写链、首层 consumer 治理卡、archive/runtime 证据摘要与实例跳转 | 继续补更深 lineage 可视化与真容器 live 证据 |
| P0-03 | 真 Docker per-run 联调 | 代码路径已在，daemon 未运行 | 启动 Docker daemon，构建 runner image，验证单 run 容器启动、挂载、回收、资源限制与证据归档 |
| P0-04 | 真 Codex CLI 与第三方 MCP 联调 | bridge/worker 代码已就位，缺实机证据 | 在容器内验证 Codex CLI 长会话、文件写出、恢复；验证不受控第三方 MCP 探活、超时、熔断、审计 |
| P0-05 | 多仓库独立交付 | 仍依赖 `workspace:*` | 设计共享包发布方案，支持五个远端仓库脱离根工作区后独立安装、构建、测试、发布 |
| P0-06 | 本地完整联调栈 | compose/infra 脚本已在，未形成实机证据 | 跑通 PostgreSQL、Redis、MinIO、API、Worker、前端联调，补环境说明与回归脚本 |
| P0-07 | 发布/回滚/上线 Gate | 当前只有 CI build/test 门禁 | 增加镜像发布、migration gate、配置切换、回滚剧本、上线检查与失败回退 |
| P0-08 | 双前端业务级 E2E | 当前为 `9` 条 smoke，仍属于导航与局部交互级验证 | 补登录、工作区切换、工坊启动、任务对话、审批、文件下载、Creator 发布、治理审批等全链验收 |

### 7.2 P1 上线前补齐

| 编号 | 开发项 | 当前状态 | 具体开发内容 |
|---|---|---|---|
| P1-01 | Dashboard 批量启动与批次看板产品化 | 基础批次域、基础看板、CSV/XLSX 导入接口、工作表切换、显式字段映射、治理输入、预算预估、基础动作与正式持久化已实现 | 补更细治理与业务级交互 |
| P1-02 | `packages/files` | 不存在 | 收敛对象存储访问、preview/download helper、ticket、path policy、checksum、retention |
| P1-03 | `packages/realtime` | 不存在 | 收敛 WS/SSE 协议、resume token、reconnect/backlog、客户端 SDK |
| P1-04 | `packages/shared` | 不存在 | 收敛 logger、error、id、retry、telemetry、feature flag |
| P1-05 | Dashboard 统一组件层 | 组件仍以内嵌页面为主 | 下沉 Drawer、Tabs、MessageCard、FileTree、Skeleton、ErrorBoundary |
| P1-06 | Mobile 平台特化 | 当前仅 H5 | 适配 `weapp` / `alipay` 上传、下载、预览、分享与权限差异 |
| P1-07 | 文件预览矩阵与附件恢复 | H5 已可用，覆盖不完整 | 完整支持 Office/PDF/图片/二进制预览与附件跨页面恢复 |
| P1-08 | 观测与告警 | 有 diagnostics/metrics 位，缺正式运维链 | Prometheus、OTel、Loki/Grafana、告警与 trace 传播 |

### 7.3 P2 质量增强

| 编号 | 开发项 | 当前状态 | 具体开发内容 |
|---|---|---|---|
| P2-01 | 性能预算与缓存治理 | 未系统化 | 首屏体积、路由拆分、缓存失效、资源体积约束 |
| P2-02 | 结果包衍生运行 | 未系统化 | 结果复用、再次运行、批量重跑、派生任务 |
| P2-03 | 企业审计导出与运营分析 | 基础域已在 | 导出格式、签名、留存策略、Creator 发布转化与成本分析 |

## 8. 组件级差距

| 组件 | 当前事实 | 主要差距 |
|---|---|---|
| `app/api` | 正式域最完整，`57/57` 全量 smoke 通过且 consumer inherit 后端主链已落地，session archive 正式导出已补 runtime-derived snapshot 来源并切入 objectStore 字节持久化，元数据索引也已支持 `file/postgres` 双轨，batch-runs 基础域、预算预估、CSV/XLSX 导入解析与 PostgreSQL 持久化已落地；run 聚合已新增 `informationCollection` 结构化状态并验证附件 `slotKey` 绑定 file 槽位 | 共享数据层继续收敛、文本型槽位答案正式化、真实对象存储/S3 联调与 live Docker/Codex 实机证据仍缺 |
| `app/run-worker` | `27/27` 测试通过，session-pack materializer 已落地 | 真 daemon、真容器、runner image 实机验证缺失 |
| `app/container-bridge` | `23/23` 测试通过，CLI/proxy/materializer 主链已在 | 真 Codex CLI、真第三方 MCP、浏览器 profile 生命周期未验收 |
| `app/dashboard` | build 通过，Creator Session 已接正式 API、lineage、consumer 派生识别与基线差异视图，并补齐 consumer-derived create、`unpublish` 写链、首层 consumer 治理卡、archive/runtime 证据摘要与实例跳转；服务详情已接批次草稿、CSV/XLSX 导入解析、工作表切换、显式字段映射、治理输入、预算预估与批次看板 | 更深 lineage 可视化、正式持久化与业务级 E2E 未完成 |
| `app/mobile` | `build:h5` 通过，任务对话与文件页主链已成型 | `weapp/alipay`、附件恢复、预览矩阵、业务级 E2E 未完成 |
| `packages/session-pack` | pack/unpack/validate/archive/restore 已成型，正式 session archive 已能承接 imported、runtime-derived 与 generated 三层来源 | 生产态脱敏、slot、签名、容器内 live 消费证据缺失 |
| `packages/db` | 已超出最小 skeleton | 进一步统一正式仓储与 query/repository 主链 |

## 9. 前端与测试的真实边界

| 项 | 当前事实 | 结论 |
|---|---|---|
| Dashboard 路由 | 当前已有 workshops / services / services/:serviceId/batches/:batchJobId / instances / creator 主链 | 批次深链已进入正式路由层，仍缺独立批次列表、导入向导与更深治理页面 |
| Mobile 页面 | 当前只有工坊、任务、文件、我的主链 | 小程序端特化尚未开始 |
| Playwright E2E | 当前为 `dashboard 5 + mobile 4 = 9` 条 smoke | 仍属于导航与局部交互验证，未形成业务级验收矩阵 |
| Session 消费继承展示 | 后端已可自动派生 consumer session-pack，Dashboard Creator 已显示 inheritMode、consumer 元数据、lineage 标签、基线差异视图、archive/runtime 证据摘要，并支持显式创建 consumer-derived 版本、`unpublish`、实例跳转与 consumer 治理快捷派生草稿 | 更深 lineage 可视化与真容器 live 证据仍未完成 |

## 10. 收敛顺序建议

| 顺序 | 任务 | 目标 |
|---|---|---|
| 1 | 补消费态治理面与 live 证据 | 把 Session Workshop 从主写链打通推进到真实消费闭环 |
| 2 | 打通真 Docker + 真 Codex CLI + 真第三方 MCP | 建立平台核心执行证据 |
| 3 | 解决五仓库独立交付与本地完整联调 | 让工程具备真实交付条件 |
| 4 | 完成 Dashboard 批量能力与双前端业务级 E2E | 收口重度用户和轻度用户验收链 |
| 5 | 补共享包与运维链 | 进入正式上线准备阶段 |

# 灵办词元 测试验证与用例矩阵总表

## 1. 文档信息

| 字段 | 内容 |
|---|---|
| 文档名称 | 测试验证与用例矩阵总表 |
| 文档类型 | Test, Verification & Case Matrix |
| 适用范围 | `agent-workshop` 全工作区 |
| 基准日期 | 2026-07-08 |
| 关联文档 | `docs/联调与验收清单.md`、`docs/工程状态总表.md`、`docs/风险与成本监控表.md` |
| 使用目的 | 将当前已验证证据、现有自动化缺口、推荐测试分层、关键用例、环境矩阵与 CI 关卡统一表格化 |

## 2. 当前可验证证据总表

| 验证项 | 当前命令 | 当前结果 | 证据日期 |
|---|---|---|---|
| Shared Packages 构建 | `pnpm -C . build:shared` | 通过 | 2026-07-08 |
| Backend 类型检查 | `pnpm -C . typecheck:backend` | 通过 | 2026-07-08 |
| Backend 聚合构建 | `pnpm -C . build:backend` | 通过 | 2026-07-08 |
| API 构建 | `pnpm -C app/api build` | 通过 | 2026-07-08 |
| API 文件链集成烟测 | `pnpm -C app/api test:smoke` | 通过 | 2026-07-08 |
| Run Worker 构建 | `pnpm -C app/run-worker build` | 通过 | 2026-07-08 |
| Container Bridge 构建 | `pnpm -C app/container-bridge build` | 通过 | 2026-07-08 |
| Dashboard 构建 | `pnpm -C app/dashboard build` | 通过 | 2026-07-08 |
| Mobile H5 构建 | `pnpm -C app/mobile build:h5` | 通过 | 2026-07-08 |
| Docker Runtime 可用性 | `docker version` | Client 可用，daemon 未启动 | 2026-07-08 |

## 3. 当前自动化测试资产盘点表

| 路径 | 类型判断 | 当前结论 |
|---|---|---|
| `app/api/tests/file-chain.smoke.test.mjs` | Node 集成烟测 | 真实测试文件，覆盖 file-backed upload preview、runtime-output objectify、workspace 回收后的 index/object-store 回退主链 |
| `app/api/tests/file-chain-postgres.smoke.test.mjs` | Node 集成烟测 | 真实测试文件，覆盖 postgres-backed repository/event-bus、`auth refresh`、`upload finalize`、runtime-output objectify 与 workspace 回收回退主链 |
| `app/run-worker/src/services/specs.ts` | 运行时 schema 定义文件 | 名称含 `specs`，不属于测试文件 |
| `app/mobile/.env.test` | 测试环境配置文件 | 不是测试用例 |
| 其余 `app/**`、`packages/**` | `rg --files -g "*test*" -g "*spec*"` 结果基本为空 | 除 API 烟测外，当前仍基本无真正测试文件 |

## 4. 当前验证层级现状表

| 层级 | 当前状态 | 说明 |
|---|---|---|
| 单元测试 | 基本缺失 | 无成体系正式 `*.test.*` / `*.spec.*` 单元用例 |
| 集成测试 | 初步存在 | `app/api/tests/file-chain*.smoke.test.mjs` 已覆盖 file-backed 与 postgres-backed 文件主链 |
| E2E | 缺失 | 无 工坊 -> 启动 -> 对话 -> 文件 下载 主链自动化 |
| 构建验证 | 已有 | Shared Packages、API、Worker、Bridge、Dashboard、Mobile H5 均可构建 |
| 类型验证 | 已有 | Backend 主链通过，多个仓库类型检查隐含于 `tsc build` |
| 原型验证 | 已有 | 前端 demo 与工程页面可人工查看 |

## 5. 推荐测试分层总表

| 测试层 | 目标 | 优先级 |
|---|---|---|
| Schema / Domain 单元测试 | 保证 contracts 和状态机稳定 | P0 |
| API 服务单元测试 | 保证 run service、file-access、error normalize 行为稳定 | P0 |
| Worker 物料生成测试 | 保证 workspace、runtime、container plan 输出稳定 | P0 |
| Bridge 组件单元测试 | 保证 secret/MCP/materializer/event parser 行为稳定 | P1 |
| API 集成测试 | 保证 HTTP / WS / SSE 行为一致 | P1 |
| 前端 adapter 测试 | 保证 `RunSnapshot -> ViewModel` 稳定 | P1 |
| E2E 主链测试 | 保证用户主路径可用 | P1 |
| 容量与故障演练 | 保证生产治理能力 | P2 |

## 6. Repo 级当前覆盖度矩阵表

| 仓库/目录 | 类型检查 | 构建验证 | 单元测试 | 集成测试 | E2E | 当前判断 |
|---|---:|---:|---:|---:|---:|---|
| `app/api` | 是 | 是 | 否 | 是 | 否 | 已有 file-backed 与 postgres-backed 文件链集成烟测，其他行为覆盖仍不足 |
| `app/run-worker` | 是 | 是 | 否 | 否 | 否 | 运行物料生成缺自动化验证 |
| `app/container-bridge` | 是 | 是 | 否 | 否 | 否 | 会话与控制面缺自动化验证 |
| `app/dashboard` | 隐含于 build | 是 | 否 | 否 | 否 | 页面结构可构建，真实行为未验证 |
| `app/mobile` | 隐含于 build | 是 | 否 | 否 | 否 | H5 可构建，真实行为未验证 |
| `packages/contracts` | 是 | 是 | 否 | 不适用 | 不适用 | schema 无用例 |
| `packages/domain-models` | 是 | 是 | 否 | 不适用 | 不适用 | 状态机无用例 |
| `packages/api-sdk` | 隐含于 build | 是 | 否 | 否 | 否 | 客户端门面已可构建，但无用例 |
| `packages/ui-tokens` | 隐含于 build | 是 | 否 | 不适用 | 不适用 | 共享样式 token 可构建，但无用例 |

## 7. Backend P0 单元测试矩阵表

| 模块 | 用例 | 期望结果 |
|---|---|---|
| `AppError` | `ZodError -> VALIDATION_ERROR` | 返回 400 和 flattened details |
| `AppError` | `AppError -> payload` | 返回原始 code / message / details |
| `RunsService` | `createRun` | 创建 run、seed message、seed files、seed approval |
| `RunsService` | 非法状态流转 | 抛 `RUN_STATUS_INVALID` |
| `RunsService` | `approve(true)` | 状态回到 `RUNNING` 并写 approval message |
| `RunsService` | `approve(false)` | 状态转 `CANCELLED` |
| `RunsService` | `ingestBridgeEvents` 去重消息 | 同 `messageId` 不重复写入 |
| `RunFileAccessService` | target path 越界 | 抛 `FILE_PATH_INVALID` |
| `RunFileAccessService` | 目录读取 | 抛 `FILE_PATH_INVALID` |
| `RunFileAccessService` | 文件不存在 | 抛 `FILE_NOT_FOUND` |

## 8. Contracts / Domain P0 测试矩阵表

| 模块 | 用例 | 期望结果 |
|---|---|---|
| `contracts/common` | ID 前缀校验 | 非法前缀无法通过 parse |
| `contracts/runs` | `CreateRunInput` 默认值 | `initialMessage`、`bindings` 默认值生效 |
| `contracts/bridge` | `BridgeEvent` discriminated union | 非法事件类型 parse 失败 |
| `contracts/realtime` | `runs.error` / `runs.snapshot` parse | 协议兼容 |
| `domain-models/runs` | 合法状态流转 | `canTransitionRunStatus` 为 true |
| `domain-models/runs` | 非法状态流转 | `assertRunStatusTransition` 抛错 |
| `domain-models/runs` | `applyBridgeEventToRunSnapshot` | 正确投影 message / approval / artifact / files |
| `domain-models/runs` | `createInformationCollectionPrompt` | prompt 包含标题和目标路径 |

## 9. Worker P0 测试矩阵表

| 模块 | 用例 | 期望结果 |
|---|---|---|
| `workspace-preparer` | 生成 host/container 路径 | 路径完整且目录被创建 |
| `run-lifecycle` | host/container secret 路径改写 | host 与 container 路径正确映射 |
| `container-runtime` | env 生成 | 包含 `RUN_ID`、`WORKSPACE_ID`、`TARGET_PATH` 等 |
| `container-runtime` | secret manifest 生成 | env/file 两类 secret 正确分流 |
| `container-runtime` | MCP config 生成 | stdio / remote-managed / remote-unmanaged 正确物化 |
| `container-runtime` | launch plan 生成 | mounts、labels、resources、commandPreview 完整 |
| `start-run` | 正常启动作业 | 输出 `READY`、`QUEUED` 两个状态事件 |

## 10. Container Bridge P1 测试矩阵表

| 模块 | 用例 | 期望结果 |
|---|---|---|
| `secret-loader` | 缺 secret value | 抛明确错误 |
| `mcp-materializer` | env/file auth 模式 | 正确写入 `auth_env` / `auth_file` |
| `event-parser` | stdout 文本解析 | 转成结构化事件 |
| `file-watcher` | 文件新增/修改 | 发出 `file.changed` / `files.synced` |
| `artifact-publisher` | outputs 刷新 | 正确生成产物事件 |
| `run-control-server` | sendMessage/approve/cancel | 正确委托下游组件 |
| `codex-session` | 未启动即发送消息 | 抛当前错误 |

## 11. API 集成测试矩阵表

| 接口 | 关键用例 | 期望结果 |
|---|---|---|
| `POST /v1/runs` | 最小合法输入创建 run | 返回 `CreateRunResponse` |
| `GET /v1/runs/:runId` | run 存在 | 返回 `RunSnapshot` |
| `GET /v1/runs/:runId` | run 不存在 | 返回 404 + `RUN_NOT_FOUND` |
| `POST /v1/runs/:runId/messages` | 发送消息 | 消息写入快照并异步分发 bridge |
| `POST /v1/runs/:runId/approvals` | 审批通过 | 状态变化和消息写入 |
| `GET /v1/runs/:runId/files/read` | 文本预览 | 返回 `RunFileReadResponse` |
| `GET /v1/runs/:runId/files/download` | 下载文件 | 返回 stream 和正确 header |
| `POST /v1/runs/:runId/uploads` + preview/ticket | 上传附件与回源下载 | 上传件可 finalize、preview 并以 `uploaded-object` ticket 下载 |
| runtime output -> ticket download | 运行产物对象化 | 写入 `targetPath/output/*` 后可入 `run_files`、写 object storage 并以 `object-store` ticket 下载 |
| `GET /v1/runs/:runId/stream` | 建立 SSE | 首帧 snapshot，后续 runs.event |
| `WS /ws/runs/:runId` | runs.subscribe | 返回 snapshot / ack |
| `WS /ws/runs/:runId` | runId 不一致 | 返回 `runs.error` |

## 12. Frontend Adapter 测试矩阵表

| 模块 | 用例 | 期望结果 |
|---|---|---|
| `mapRunSnapshotToInstanceRecord` | `RUNNING` snapshot | status / nextAction / metrics 正确 |
| `mapRunSnapshotToInstanceRecord` | `WAITING_APPROVAL` snapshot | `statusClass=warn`，展示审批动作 |
| `mapRunSnapshotToMobileTask` | `SUCCEEDED` snapshot | 生成结果模块消息 |
| `mapRunSnapshotToMobileTask` | `FAILED` snapshot | 生成错误恢复模块消息 |
| `buildDashboardRunInput` | 各 serviceId | 生成正确 `taskVersionId / sessionVersionId / bindings` |
| `buildMobileRunInput` | 各 serviceId + workspace | 生成正确 runtime `workspaceId` 和 `targetPath` |

## 13. Realtime 关键场景测试矩阵表

| 场景 | 验证点 |
|---|---|
| WS 首次连接 | 自动发送 `runs.subscribe`，收到 snapshot + ack |
| WS 增量事件 | event 投影进入 Query Cache |
| SSE fallback | 无 WebSocket 时可接收 snapshot + event |
| 断连重连 | 前端重新订阅并恢复 snapshot |
| 重复消息 | 同 `messageId` 不重复渲染 |
| 审批后继续运行 | 状态从 `WAITING_APPROVAL -> RUNNING` |

## 14. 文件链与权限测试矩阵表

| 场景 | 期望结果 |
|---|---|
| 浏览 target path 根目录 | 可返回目录树 |
| 请求越界路径 | 被拒绝，返回 `FILE_PATH_INVALID` |
| 读取图片/PDF | 文本读取不适用，下载通道可用 |
| 下载目录 | 被拒绝 |
| 文件变更推送 | 前端文件页实时刷新 |

## 15. 审批与高风险动作测试矩阵表

| 场景 | 期望结果 |
|---|---|
| 运行中触发审批 | run 进入 `WAITING_APPROVAL` |
| 审批通过 | run 继续 `RUNNING` |
| 审批拒绝 | run 转 `CANCELLED` |
| 同一审批重复处理 | 正式实现后应拒绝并记录冲突 |
| 高风险 MCP 首次调用 | 正式实现后应触发审批与审计 |

## 16. 隔离与安全演练矩阵表

| 场景 | 目标 |
|---|---|
| run A 尝试读取 run B 目录 | 失败 |
| `/workspace/secrets` 对终端用户不可浏览 | 失败或隐藏 |
| 共享凭证未授权用户不可使用 | 失败并记审计 |
| 第三方 MCP 越权出网 | 被网络策略拒绝 |
| 目标路径越界下载 | 被拒绝 |

## 17. 故障与恢复演练矩阵表

| 故障场景 | 期望恢复行为 |
|---|---|
| API 重启 | 文件型 repository 与 event bus 能从磁盘恢复 |
| Bridge 失联 | run 标记失败或进入恢复流程 |
| Worker 启动 bridge 超时 | 抛 `RUNTIME_BRIDGE_TIMEOUT` 等正式错误码 |
| 对象存储上传失败 | 产物事件不应标记 ready，需重试或告警 |
| 第三方 MCP 不可用 | 错误被投递到实例对话与审计 |

## 18. 性能与容量测试矩阵表

| 指标 | 建议测试 |
|---|---|
| 单 run 文件树枚举 | 大目录下 `files/tree` 延迟与内存 |
| 多 run backlog | `runEventBus.list()` 历史长度对内存影响 |
| Realtime 连接数 | 多 WS/SSE 连接稳定性 |
| Artifact 目录变化频率 | watcher 去抖与事件风暴 |
| 大文件下载 | stream 稳定性与权限开销 |

## 19. CI / 验收关卡建议表

| 阶段 | 最低要求 |
|---|---|
| PR 级 | lint + typecheck + unit tests |
| Merge 级 | backend build + dashboard build + mobile build |
| Nightly | API 集成测试 + adapter 测试 |
| Milestone | E2E 主链 + 容器隔离演练 + 人工验收 |
| Release | 审计、成本、权限、回滚演练 |

## 20. 测试夹具与模拟依赖总表

| 夹具/模拟对象 | 用途 |
|---|---|
| 假 `RunSnapshot` 工厂 | adapter 和 domain 投影测试 |
| 假 `BridgeEvent` 工厂 | event 投影和 realtime 测试 |
| 临时文件系统目录 | file-access / watcher / runtime 物料测试 |
| 假 `fetch` / 假 `WebSocket` | SDK 测试 |
| 假 secret map | secret-loader 测试 |
| 假 MCP bindings | materializer 测试 |
| 假 worker payload | `startRunJob` 测试 |

## 21. 当前缺口与补齐优先级表

| 优先级 | 缺口 | 原因 |
|---|---|---|
| P0 | contracts + domain + runs service + file-access 单元测试 | 风险最高，改动频繁 |
| P0 | worker runtime 物料生成测试 | 后续 Docker 接入前必须稳定 |
| P1 | realtime / SDK / adapter 测试 | 直接影响前后端联调 |
| P1 | API 集成测试 | 保证 HTTP / WS / SSE 行为一致 |
| P2 | 容量、隔离、故障演练自动化 | 进入共享环境与生产前补齐 |

## 22. 当前已验证事实表

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 构建链 | 五个独立仓库与四个共享包均可构建 | 本轮 `pnpm build` 命令结果 |
| 后端主链 | `typecheck:backend` 已通过 | `pnpm -C . typecheck:backend` |
| 测试资产 | 当前基本无真实测试文件 | `rg --files app packages -g "*test*" -g "*spec*"` |
| 前端原型 | Dashboard 与 Mobile H5 可构建输出静态产物 | `pnpm -C app/dashboard build`、`pnpm -C app/mobile build:h5` |
| Runtime 真执行 | 当前无法本机自动验证 | `docker version` 显示 daemon 未启动 |

## 23. 当前不可自动验证项表

| 领域 | 当前不可验证原因 | 影响 |
|---|---|---|
| Worker -> Runner Image 真执行链 | Docker daemon 未启动 | 无法产出容器执行烟测证据 |
| Bridge 容器内回调链 | 无真容器运行证据 | 无法验证事件回流、控制面、文件监听的容器态表现 |
| 对象存储文件链 | 无正式对象存储 | 无法验证签名下载、上传、版本化 |
| 鉴权与租户隔离 | 无正式 auth / ACL | 无法验证多用户安全边界 |
| Shared Dev / Staging 验收 | 环境不存在 | 无法验证多人联调与预发布行为 |

## 24. 首批应落测试文件矩阵表

| 路径建议 | 测试类型 | 首批覆盖对象 | 进入条件 |
|---|---|---|---|
| `packages/contracts/src/**/*.test.ts` | 单元测试 | schema parse、默认值、判别联合 | 立即可开始 |
| `packages/domain-models/src/**/*.test.ts` | 单元测试 | run 状态机、事件投影、prompt 组装 | 立即可开始 |
| `app/api/src/modules/runs/**/*.test.ts` | 单元测试 | `RunsService`、`RunFileAccessService`、error normalize | 立即可开始 |
| `app/run-worker/src/services/**/*.test.ts` | 单元测试 | workspace、runtime config、launch plan | 立即可开始 |
| `app/container-bridge/src/**/*.test.ts` | 单元测试 | secret loader、materializer、event parser、control server | P0 完成后进入 |
| `app/api/test/**/*.test.ts` | 集成测试 | HTTP / WS / SSE | 需要临时数据目录与 server fixture |
| `app/dashboard/src/**/*.test.tsx` | adapter/component 测试 | run view model、语言切换、状态渲染 | API SDK 基线稳定后进入 |
| `app/mobile/src/**/*.test.tsx` | adapter/component 测试 | mobile task mapping、文件页状态、任务消息渲染 | H5 真实数据接线稳定后进入 |

## 25. 测试落地顺序与门禁升级表

| 阶段 | 先补内容 | 升级后的门禁 |
|---|---|---|
| Phase 1 | contracts/domain/api/worker P0 单元测试 | PR 级必须通过单元测试 |
| Phase 2 | bridge 单元测试 + API 集成测试 | backend PR 级进入集成门禁 |
| Phase 3 | dashboard/mobile adapter 测试 | 前端 PR 级进入行为验证 |
| Phase 4 | E2E 主链 + runtime 烟测 | Milestone 级必须通过端到端 |
| Phase 5 | 隔离、故障、容量演练 | Release 级必须具备生产门禁 |

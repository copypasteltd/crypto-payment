# 灵办词元 仓库完成度与模块状态表

## 1. 文档信息

| 字段 | 内容 |
|---|---|
| 文档名称 | 仓库完成度与模块状态表 |
| 统计日期 | 2026-07-12 |
| 统计范围 | `agent-workshop` 根工作区、五个独立仓库、现有共享包 |
| 统计口径 | 以当前代码目录、Git 提交、构建结果、测试结果、文档约束与显式缺口为准 |
| 说明 | 本文档用于回答“每个仓库当前完成度 / 已实现模块 / 未完成模块” |

## 2. 当前权威基线

| 维度 | 当前事实 |
|---|---|
| 根路径 | `C:\dev\copypaste\agent-workshop` |
| 根工作区 Git | 根路径本身不是 Git 仓库 |
| 独立仓库 | `app/mobile`、`app/dashboard`、`app/api`、`app/run-worker`、`app/container-bridge` 均有独立 `.git` |
| 当前提交 | `mobile=af0b2ae`、`dashboard=322b4e1`、`api=372bc23`、`run-worker=78b1de7`、`container-bridge=92876c8` |
| 工作树状态 | 五个独立仓库均存在未提交变更，当前不是可直接推送的干净基线 |
| 共享包现状 | 已有 `api-sdk`、`config`、`contracts`、`credential`、`db`、`domain-models`、`files`、`mcp`、`realtime`、`session-pack`、`shared`、`ui-tokens` |
| 缺失共享包 | 无 |
| 根级验证 | `pnpm ci:verify` 已通过，且当前采用 compiled-test 口径 |
| 后端 smoke | `app/api` 本轮最新全量复验 `57/57` 通过 |
| Runtime tests | `app/run-worker` `27/27`，`app/container-bridge` `23/23` |
| Frontend E2E | Playwright `23/23`，当前为 smoke 主链覆盖，并已纳入 Dashboard / Mobile `plain-text / office-text / image / pdf / download-only` 文件预览下载矩阵、附件上传发送主链、Dashboard 批量运行生命周期，以及 Dashboard Creator `session-pack` 敏感位补标、脱敏复核决策、脱敏归档导出审计与 publish / inherit / rollback 用例 |
| CI 验证链 | 根级 `ci:backend / ci:runtime / ci:verify` 已收敛为单次编译后复用 `test:smoke:compiled / test:compiled` |
| 本地虚拟化约束 | 当前明确禁止调度任何本地 `docker / wsl`，因此真容器链不以本地环境验收 |

## 3. 仓库总览

| 仓库 | 本地路径 | 远端仓库 | 当前提交 | 角色 | 主栈 |
|---|---|---|---|---|---|
| `agent-workshop-app` | `app/mobile` | `git@github.com:copypasteltd/agent-workshop-app.git` | `af0b2ae` | 首发轻用户入口，Taro H5，后续扩展微信/支付宝小程序 | `Taro 4 + React 18 + TypeScript` |
| `agent-workshop-dashboard` | `app/dashboard` | `git@github.com:copypasteltd/agent-workshop-dashboard.git` | `322b4e1` | 重度用户 / Creator / Admin 控制台 | `React 18 + Vite 5 + TypeScript` |
| `agent-workshop-backend` | `app/api` | `git@github.com:copypasteltd/agent-workshop-backend.git` | `372bc23` | API、Realtime、治理、读模型、运行编排入口 | `Fastify 5 + TypeScript` |
| `agent-workshop-run-worker` | `app/run-worker` | `git@github.com:copypasteltd/agent-workshop-run-worker.git` | `78b1de7` | runtime 物料、队列消费、容器调度、恢复与诊断 | `TypeScript + BullMQ + Zod` |
| `agent-workshop-sdk` | `app/container-bridge` | `git@github.com:copypasteltd/agent-workshop-sdk.git` | `92876c8` | 容器内 Bridge、Codex CLI 接线、文件回流、MCP 代理 | `TypeScript + node-pty + chokidar` |

## 4. 当前完成度总表

说明：

- `代码主干完成度` 代表功能骨架、主路径、关键模块的落地比例。
- `交付就绪度` 代表距离最终成品、可上线、多用户、可回滚系统的接近程度。
- 分值为当前工程判断，不等于自动化测试覆盖率。

| 仓库 | 代码主干完成度 | 交付就绪度 | 当前判断 |
|---|---:|---:|---|
| `agent-workshop-app` | 91% | 66% | H5 live 主链已覆盖工坊/任务/文件/我的，任务对话已具备本地回显/outbox/ack/失败重试，任务页已显性展示 `informationCollection` 缺口并接入结构化答案复核，启动 -> 首轮信息收集 -> 审批 / 答案复核 smoke E2E 已补齐，仍缺小程序特化与业务级 E2E |
| `agent-workshop-dashboard` | 95% | 84% | Creator、实例、治理主壳已形成，运行时权威数据主链已收口，`Session` 面板已对齐正式 session-pack contract，并接通 inherit draft / consumer-derived create / publish / unpublish / rollback / archive download、消费派生识别、lineage 与基线差异视图、consumer governance 摘要 / 清单、session governance summary（状态 / 风险 / 下线风险 / 基线差异字段 / 派生计数）、signature governance summary（算法 / key / 验签状态 / 兼容性诊断）、policy summary（动作级放行 / 警告 / 阻断、脱敏复核门禁、覆盖完整性门禁、验签门禁）、archive/runtime 证据摘要、信息采集 review 摘要、手工敏感位补标与实例跳转；服务启动台已具备批次草稿、批次看板、CSV/XLSX 导入解析、工作表切换、显式字段映射、治理输入与预算预估；实例页已显性展示 `informationCollection` 缺口并接入结构化答案复核，启动 -> 首轮信息收集 -> 审批 / 答案复核 smoke E2E 已补齐，实例文件预览 + 下载 ticket + 附件上传/发送主链回归也已补齐，Creator 侧 `session-pack` 敏感位补标、脱敏复核决策、脱敏归档导出审计、治理摘要展示以及 publish / inherit / rollback 业务级 E2E 也已补齐一条主链；仍缺更深 lineage 可视化、正式持久化与更广业务级 E2E |
| `agent-workshop-backend` | 99% | 95% | API 正式域最完整，核心缺口转向共享数据层、session-pack、部署与实机证据 |
| `agent-workshop-run-worker` | 92% | 67% | Docker create/start 逻辑与恢复框架已在，缺真 daemon 联调与 runner image 证据 |
| `agent-workshop-sdk` | 91% | 61% | Bridge 主体能力已在，缺真 Codex CLI、第三方 MCP、浏览器能力实机证据 |

## 5. 共享包状态

### 5.1 已存在共享包

| 包 | 当前作用 | 当前状态 |
|---|---|---|
| `@lingban/api-sdk` | 双前端访问 API 的 SDK | 已形成 auth / catalog / runs / creator / governance / search 接入层 |
| `@lingban/config` | 配置装载与环境变量解析 | 已承担后端 / worker / bridge 大量配置入口 |
| `@lingban/contracts` | 领域契约与 schema | 已覆盖 runs / bridge / runtime / search / credential 等主契约 |
| `@lingban/credential` | 凭证辅助逻辑 | 已形成 redaction、挂载、运行时辅助 |
| `@lingban/db` | 共享 PostgreSQL manager / migration / transaction 基础层 | 已形成 `createPostgresDatabaseManager`、共享 transaction helper、迁移加载、执行、readiness probe，以及 `AuthRepository / CreatorRepository / RunsRepository / RunEventBus / RunFilesIndexRepository / UploadRepository / RunQueryRepository / BridgeRegistrationRepository / BillingRepository / QuotaRepository / WorkshopCatalogRepository / CredentialsRepository / McpRepository / NotificationsRepository / MeRecentActivitiesRepository / MeFavoritesRepository / BatchRunsRepository / SearchRepository / SessionArchiveRecordRepository`、`CachedAuthRepository / PostgresAuthRepository`、`CachedCreatorRepository / PostgresCreatorRepository`、`CachedRunsRepository / PostgresRunsRepository`、`CachedRunEventBus / PostgresRunEventBus`、`CachedQuotaRepository / PostgresQuotaRepository`、`CachedWorkshopCatalogRepository / PostgresWorkshopCatalogRepository`、`CachedCredentialsRepository / PostgresCredentialsRepository`、`CachedMcpRepository / PostgresMcpRepository`、`PostgresRunFilesIndexRepository / PostgresUploadRepository / PostgresRunQueryRepository / PostgresBridgeRegistrationRepository / PostgresBillingRepository / PostgresNotificationsRepository / PostgresMeRecentActivitiesRepository / PostgresMeFavoritesRepository / PostgresBatchRunsRepository / PostgresSearchRepository / PostgresSessionArchiveRecordRepository` 等共享契约与实现，并已具备 `25/25` smoke |
| `@lingban/domain-models` | 运行态领域模型 | 已形成 run 状态、聚合与投影辅助 |
| `@lingban/files` | 文件域最小共享层 | 已形成 path boundary、logical path、mime/preview 推断、Office 文本抽取与 `3/3` smoke，并已接入 `app/api` 的 file access / file index / me summary 主链 |
| `@lingban/shared` | 共享基础层 | 已形成 `toErrorMessage / nowIso / buildAtomicTempPath` 与 `3/3` smoke，并已接入 `app/api`、`app/run-worker`、`app/container-bridge` 的公共错误处理、时间戳和 file-backed repository 原子临时路径主链 |
| `@lingban/mcp` | MCP 标识、绑定、策略辅助 | 已形成治理与运行时绑定辅助层 |
| `@lingban/realtime` | 双前端 run realtime 共享层 | 已收口 Dashboard / Mobile 的 run stream hook、ack 超时、snapshot 同步与 query cache 回写 |
| `@lingban/session-pack` | Session Workshop 资产包共享层 | 已形成 manifest schema、打包、目录校验、archive round-trip、structured workspace-base archive/restore、slot schema、redaction map schema、信息采集 review 摘要 root file 校验、文件级完整性校验与 `18/18` smoke，并已落地 canonical manifest signature 生成/验真；共享 contracts、`/v1/sessions` 列表/详情、`POST /v1/sessions/import`、`GET /v1/sessions/:sessionVersionId/archive`、`POST /v1/sessions/:sessionVersionId/inherit`、`POST /v1/sessions/:sessionVersionId/publish`、`POST /v1/sessions/:sessionVersionId/rollback`、`POST /v1/sessions/:sessionVersionId/unpublish`、`POST /v1/sessions/:sessionVersionId/redaction-map`、`POST /v1/sessions/:sessionVersionId/redaction-review`、Creator/Workshops/Run 启动前校验，以及 Dashboard Creator `Session` 面板 inherit draft / consumer-derived create / publish / unpublish / rollback / archive 写链、消费派生识别、基线差异视图、consumer governance 摘要 / 清单、session governance summary（状态 / 风险 / 下线风险 / 基线差异字段 / 派生计数）、signature governance summary（算法 / key / 验签状态 / 兼容性诊断）、redaction coverage 与 preview 命中摘要、redaction rule draft/edit/remove/save、手工敏感位补标、redaction review 记录、archive export audit、archive/runtime 证据摘要、信息采集 review 摘要、答案来源追踪 / 修订链 / 当前生效版本标识与实例跳转均已接入主链；正式 archive 已支持 imported、runtime-derived、generated 与 internal runtime-fallback 四层来源，且 API 侧持久化字节已切入 `objectStore`，session archive 元数据索引已支持 `file/postgres` 双轨，并已支持 `archive?redact=true` 脱敏导出与 Dashboard Creator “下载脱敏归档”入口 |
| `@lingban/ui-tokens` | 设计 token 共享层 | 已供 Dashboard 与 Mobile 主题系统使用 |

### 5.2 当前共享基础层缺口

| 包 | 已落地范围 | 当前剩余缺口 |
|---|---|---|
| `packages/shared` | `toErrorMessage / nowIso / buildAtomicTempPath` | logger、ids、retry、telemetry、feature flag 等公共基础能力仍未统一抽出 |

## 6. 各仓库详细状态

### 6.1 `agent-workshop-app` / `app/mobile`

| 类别 | 内容 |
|---|---|
| 当前定位 | 首发移动端，当前正式输出 H5，后续扩展微信小程序、支付宝小程序 |
| 已实现模块 | 登录 / 注册 / 会话恢复、工作区切换、工坊首页、工坊详情、服务详情、任务列表、任务对话页、任务文件页、我的页、最近任务、收藏摘要、通知摘要、文件下载票据、路径切换、任务内附件提交 |
| 已实现数据接线 | `auth/session`、catalog、runs、files、`/v1/me/summary`、`/v1/me/assets`、`/v1/me/authorizations`、`/v1/me/favorites/workshops`、`/v1/me/recent`、`/v1/notifications`、`/v1/search`、`/v1/search/suggestions` |
| 已实现结构能力 | Taro H5 构建主链、明暗主题、工作区上下文、任务页完整对话布局、`informationCollection` 缺口显性展示与结构化答案复核、文件页路径输入、启动 -> 首轮信息收集 -> 审批 / 答案复核 smoke 验收 |
| 未完成模块 | 附件跨页面恢复、批量下载与更完整预览矩阵、`weapp/alipay` 特化适配、统一组件层、前端埋点与漏斗、业务级 E2E |
| 当前最大缺口 | 真实任务对话与文件链已进入权威主链，剩余主要缺口是 H5 之外的平台特化与业务级验收 |

### 6.2 `agent-workshop-dashboard` / `app/dashboard`

| 类别 | 内容 |
|---|---|
| 当前定位 | 重度用户 / Creator / Admin 主控制台 |
| 已实现模块 | Dashboard Shell、抽屉式侧栏、工坊页、服务详情、实例列表、实例详情、对话区、文件区、runtime 区、audit 区、Creator 页、release / replay / gate / activation、治理面板、通知面板、账户摘要、多语言主框架、服务级批次草稿、CSV/XLSX 导入解析、工作表切换、显式字段映射与批次看板 |
| 已实现数据接线 | me / notifications / favorites / recent、catalog、runs、Creator package、release、replay、gate、activation、members、credentials、MCP governance、MCP call audit、cost / billing 摘要 |
| 已实现结构能力 | React + Vite 主工程、深链路由、Creator 分段治理、搜索入口、通知中心、明暗主题、`informationCollection` 缺口显性展示与结构化答案复核、启动 -> 首轮信息收集 -> 审批 / 答案复核 smoke 验收 |
| 未完成模块 | 更深 lineage 可视化、正式持久化、统一组件层、统一骨架/空态/错误态、埋点与漏斗、业务级 E2E |
| 当前最大缺口 | Creator 已有强骨架，实例/治理主链也已切到权威数据；平台核心剩余缺口集中在更深 lineage 可视化、批量能力与真容器执行证据层 |

### 6.3 `agent-workshop-backend` / `app/api`

| 类别 | 内容 |
|---|---|
| 当前定位 | 平台中枢 API、治理面、读模型、运行入口 |
| 已实现模块 | auth、workspaces、workspace members、workspace invitations、workshops、creator、releases、replays、uploads、runs、realtime、internal bridge、sessions、batch-runs、credentials、MCP registry/bindings/policies/probe/audit、billing、quotas、me、notifications、search |
| 已实现基础设施能力 | 正式 migrations `0001`-`0026`、对象存储驱动、下载票据、文件预览、安全扫描、冷归档、审计导出、health/metrics/readiness、credential broker、callback auth/signing、AWS KMS、Vault、mTLS、OAuth callback、session archive objectStore 持久化字节链与 `file/postgres` 双轨元数据索引 |
| 已实现验证 | `app/api test:smoke` 本轮最新全量复验 `57/57` 通过 |
| 未完成模块 | `packages/db` 继续扩展 query repository/更深正式读模型与边界/seed 数据治理、`packages/session-pack` 远端隔离运行时 live consumption 实机证据、部署/回滚链、真实对象存储/S3 实机联调、远端容器 + live Codex CLI 全链证据 |
| 当前最大缺口 | 代码最完整，缺口主要集中在 `packages/db` 深化收敛、`session-pack` 生产闭环、部署化、实机化四个层面 |

### 6.4 `agent-workshop-run-worker` / `app/run-worker`

| 类别 | 内容 |
|---|---|
| 当前定位 | 队列消费、runtime 物料、容器调度、运行恢复 |
| 已实现模块 | runtime 目录准备、workspace 准备、Docker create/start/inspect/stop/rm、BullMQ 消费、DLQ、cleanup、recovery、worker ops HTTP、diagnostics、metrics、runtime egress proxy hook、egress firewall plan、runtime user/umask/secret permission |
| 已实现验证 | `app/run-worker` tests `27/27` |
| 未完成模块 | 真 Docker daemon 联调、runner image 构建与实测、真实资源限制验证、跨节点补偿、容器运行证据归档 |
| 当前最大缺口 | 运行逻辑已经具备平台意图，但缺少真容器执行证据，当前仍属于“代码完成，环境未验” |

### 6.5 `agent-workshop-sdk` / `app/container-bridge`

| 类别 | 内容 |
|---|---|
| 当前定位 | 容器内 Bridge，负责 Codex CLI、PTY、MCP、文件与控制回流 |
| 已实现模块 | PTY、事件解析、control HTTP、diagnostics、metrics、artifact 回流、Codex session recovery、queued input replay、MCP materializer、remote MCP proxy、runtime network policy enforcement、stdio allowlist 校验、digest 校验、internal credentials materialize 接口消费 |
| 已实现验证 | `app/container-bridge` tests `23/23` |
| 未完成模块 | 容器内真实 Codex CLI 实机验证、真实第三方 MCP 联调、浏览器 profile 生命周期验证、跨重启恢复增强 |
| 当前最大缺口 | 桥接能力很强，但最关键的“真容器 + 真 Codex + 真第三方连接器”还未形成交付证据 |

## 7. 根工作区级问题

| 问题 | 当前状态 | 影响 |
|---|---|---|
| 根路径不是 Git 仓库 | 当前成立 | `docs/`、`infra/`、`packages/`、根级 `tests/` 缺统一远端版本治理 |
| 五仓库依赖 `workspace:*` | 当前成立 | 五个远端仓库脱离根工作区后不能独立构建 |
| 关键共享包收口未完成 | 部分成立 | `packages/db`、`files`、`shared`、`session-pack` 与 `realtime` 均已接入正式主链的一部分，其中 `files` 已收口 path boundary、logical path、mime/preview 推断与 Office 文本抽取，`shared` 已收口公共错误处理、时间戳与 file-backed repository 原子临时路径，`realtime` 已收口双前端 run stream hook；剩余缺口集中在更深消费态治理、API 服务端 realtime 协议共享、对象存储/ticket/retention 下沉、远端隔离运行时消费证据与更深 shared 基础设施 |
| 本地虚拟化环境禁用 | 当前成立 | 真容器链不能以本地 `docker / wsl` 方式验收 |
| 全链实机联调缺失 | 当前成立 | 不能宣称已达到最终成品交付 |

## 8. 验证基线

| 验证项 | 当前结果 | 结论 |
|---|---|---|
| `pnpm ci:verify` | 通过 | 根工作区基础回归可运行，且不再依赖子仓递归重编译 |
| `app/api test:smoke` | 本轮最新全量 `57/57` | 当前代码基线已全量通过 |
| `app/run-worker test` | `27/27` | worker 逻辑主链稳定 |
| `app/container-bridge test` | `23/23` | bridge 逻辑主链稳定 |
| Playwright E2E | `23/23` | 双前端 smoke 已覆盖 Dashboard 基础路由、深批次路由、实例 `plain-text / office-text / image / pdf / download-only` 文件预览/下载矩阵、实例附件上传/发送、批次预算估算、批次草稿创建 -> 校验 / 启动 / 重试 / 取消生命周期、服务启动 -> 首轮信息收集、Dashboard 审批直连与结构化答案复核、Dashboard Creator `session-pack` 敏感位补标、脱敏复核决策、脱敏归档导出审计与 publish / inherit / rollback，以及 Mobile 任务/文件导航、`plain-text / office-text / image / pdf / download-only` 文件预览/下载矩阵、任务附件上传/发送、发送失败重试链、我的页空间摘要、服务启动 -> 首轮信息收集、Mobile 审批直连与结构化答案复核 |
| Docker daemon | 不可用 | 真容器链未验证 |

## 9. 当前未完成模块总表

| 级别 | 模块 | 归属 |
|---|---|---|
| P0 | `packages/db` | 根工作区 / `app/api` |
| P0 | `packages/session-pack` | 根工作区 / `app/api` / `app/dashboard` |
| P0 | 真 Docker per run 实机联调 | `infra/docker` / `app/run-worker` / `app/container-bridge` |
| P0 | 容器内真实 Codex CLI 验证 | `app/container-bridge` |
| P0 | Dashboard Creator 消费继承 / lineage / 业务级 E2E | `app/dashboard` |
| P0 | 五仓库脱离根工作区可独立构建的交付方案 | 根工作区 / 五个独立仓库 |
| P1 | `packages/files` 深化 | 根工作区 / `app/api` |
| P1 | `packages/realtime` 深化 | 根工作区 / 双前端 / `app/api` |
| P1 | `packages/shared` 深化 | 根工作区 |
| P1 | 批量启动与批次看板产品化 | `app/dashboard` / `app/api` / `app/run-worker` |
| P1 | 前端组件层重构 | `app/mobile` / `app/dashboard` |
| P1 | 埋点与漏斗分析链 | 双前端 / `app/api` |
| P1 | 小程序 `weapp` / `alipay` 特化 | `app/mobile` |
| P1 | 监控、发布、回滚链 | `infra/*` / `app/api` / `app/run-worker` |

## 10. 当前结论

| 维度 | 结论 |
|---|---|
| 当前阶段 | 已超过原型阶段，属于高强度工程样机阶段 |
| 最接近成品的仓库 | `app/api` |
| 当前最关键的缺口 | `session-pack` 生产闭环、`packages/db` 深化收敛、远端隔离运行链实机证据、多仓独立交付 |
| 是否已开发结束 | 否 |
| 是否已具备最终成品交付条件 | 否 |
| 当前最合理的推进顺序 | 先补共享数据层与会话资产层，再打通真容器链与双前端 live 全链验收 |

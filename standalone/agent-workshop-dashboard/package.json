{
  "name": "agent-workshop-dashboard-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "@swc/core",
      "@tarojs/binding",
      "@tarojs/cli",
      "core-js",
      "core-js-pure",
      "esbuild",
      "msgpackr-extract",
      "node-pty"
    ]
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.17.2",
    "turbo": "^2.0.14",
    "typescript": "~5.8.3"
  },
  "scripts": {
    "dev": "pnpm -C app/dashboard run dev",
    "typecheck": "pnpm --filter @lingban/api-sdk --filter @lingban/contracts --filter @lingban/domain-models --filter @lingban/realtime --filter @lingban/ui-tokens build && pnpm -C app/dashboard run typecheck",
    "build": "pnpm --filter @lingban/api-sdk --filter @lingban/contracts --filter @lingban/domain-models --filter @lingban/realtime --filter @lingban/ui-tokens build && pnpm -C app/dashboard run build",
    "lint": "pnpm -C app/dashboard run lint",
    "preview": "pnpm -C app/dashboard run preview",
    "validate": "pnpm run lint && pnpm run build"
  }
}
